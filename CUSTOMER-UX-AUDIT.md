# CUSTOMER-UX-AUDIT — تدقيق تجربة الزبون (Diner) في BIZIII Menu

> **تقرير فقط — لم يُعدَّل أي كود.** التاريخ: 2026-06-02 · الفرع: `feat/design-redesign`.

## النطاق والمنهجية

- **السطح:** تجربة الزبون فقط (`/r/[slug]`, `/r/[slug]/p/[id]`, `/r/[slug]/cart`). لا أدمن، لا مالك.
- **الطريقة:** تشغيل التطبيق + تجوّل كامل عبر Playwright (موبايل 390×844) والتقاط **١٩ لقطة** لكل شاشة وحالة.
- **البيانات:** المطعم المزروع `origins` — **٢٣ منتجًا** (كلها بصور حقيقية)، ١١ قسمًا، ٦ أقسام فرعية، ٥ اختيارات شيف، أسماء **ثلاثية اللغة** (عربي/إنجليزي/كردي)، عملة IQD، براند زمرّدي `#0f766e`. قُلِب مؤقّتًا إلى **وضع الإغلاق** لالتقاط الخصومات ثم أُعيد إلى normal.
- **الحالات المغطّاة:** normal + closing · عربي RTL + إنجليزي LTR + كردي RTL · ترحيب + popup لغة + منيو + أقسام فرعية + تمرير/هيدر لاصق + صفحة منتج + سلة + اقتراحات + «اقرأ للنادل» + سلة فارغة + مغلق + منيو فارغ.
- **خارج النطاق (مستثنى عمدًا، لم يُسجَّل كنقص):** تسجيل دخول الزبون، الدفع، الطلب أونلاين، التوصيل، تتبّع الطلب، الحجز، التقييمات، الإشعارات.
- **منهجية التحليل:** ٧ عدسات خبيرة متوازية (findability · تصميم · حركة · وصولية · RTL/i18n · الحالات · تدفّق الشاشات) قرأت اللقطات والكود معًا، ثم توليف ودمج وترتيب. **ملاحظة شفافية:** المهارات المطلوبة (design-system، frontend-design-direction، make-interfaces-feel-better، frontend-a11y، motion-ui) **غير مُثبَّتة** في هذه البيئة؛ طُبِّقت مبادئها عبر وكلاء خبراء، واستُخدم وكيل **`a11y-architect`** المتاح فعلًا للعدسة الوصولية (WCAG 2.2 AA).
- **اللقطات:** `design-progress/customer-ux/` (انظر الجرد أدناه).
- **استُثني أيضًا** ما هو مسجّل سابقًا في `SECURITY-FINDINGS.md` و`DESIGN-PLAN.md` (إصلاحات الأمان، وبنود التدقيق المنجزة في الريديزاين: tokens، حلّ تصادم اللوحتين، أهداف ٤٤px، حلقات focus، مزامنة lang/dir، الأسهم المعكوسة، الشِبات aria-pressed، focus-trap + إعادة تركيز الحوارات).

## جرد اللقطات

| اللقطة | الشاشة |
|---|---|
| `n01-welcome-langpopup.png` | الترحيب + نافذة اختيار اللغة (أول زيارة) |
| `n02-welcome-ar.png` / `n02b-welcome-clean-ar.png` | شاشة الترحيب (عربي) |
| `n03-ar-menu.png` | المنيو RTL (عربي) — هيدر + شِبات + اختيارات الشيف + شبكة |
| `n04-en-menu.png` | المنيو LTR (إنجليزي) |
| `n05-ku-menu.png` | المنيو RTL (كردي سوراني) |
| `n06-menu-subcategories-ar.png` | المنيو مع شِبات الأقسام الفرعية |
| `n07-menu-scrolled-stickyheader-ar.png` | المنيو بعد التمرير (الهيدر اللاصق) |
| `n08-product-ar.png` / `n09-product-en.png` | صفحة المنتج (عربي / إنجليزي) |
| `n10-cart-ar.png` / `n11-cart-en.png` | السلة + الاقتراحات (عربي / إنجليزي) |
| `n12-readtowaiter-ar.png` | حوار «اطلب من الكابتن» |
| `n13-cart-empty-ar.png` | سلة فارغة |
| `c01-ar-menu.png` | منيو وضع الإغلاق (شارات −10% + أسعار مشطوبة) |
| `c02-product-discounted-ar.png` | صفحة منتج مخصوم |
| `c03-cart-discounted-ar.png` | سلة بأصناف مخصومة |
| `e01-closed-ar.png` | شاشة «المنيو غير متوفر» (مطعم معطّل) |
| `e02-empty-menu-ar.png` | منيو فارغ (مطعم نشط بلا أصناف) |

**حالتان لم تُلتقطا بصريًّا** (قيود بيئة الالتقاط، لا أخطاء منتج):
- **offline:** SW لم يتحكّم في نافذة توقيت Playwright (تحت dev = عيب Turbopack؛ في الإنتاج **يعمل** — `smoke-pwa` يمرّ ١١/١١ ويخدم HTML+API من الكاش). غُطّيت سلوكيًّا/كوديًّا أدناه (M9).
- **loading skeletons:** لا تُلتقط على مسار دافئ؛ غُطّيت من `app/r/[slug]/loading.tsx` وأخواتها (M10).

---

## سجل التنفيذ — فرع `feat/customer-ux`

> تنفيذ تلقائي بالدفعات للبنود UI-only. لقطات الإصلاحات في `design-progress/customer-ux-fixes/`. المؤجَّلات (backend/PRD): **H1، H6/F5، M1، L3** — لم تُنفَّذ (انظر القسم الأخير).

| الدفعة | البنود | الحالة |
|---|---|---|
| **1 — أخطاء حقيقية** | H8 (تأكيد قبل تفريغ السلة + إعادة تسمية)، H9 (لون مقروء على البراند)، H10 (الاسم العربي الاحتياطي لا يُعرض LTR)، +M19 (سعر شاشة الكابتن كامل التباين) | ✅ نُفِّذت — lint/tsc/build/smoke 12/12 |
| **2 — High (UI)** | H2 (بحث)، H3 (تأكيد إضافة + نبضة عدّاد + aria-live)، H4 (عملة د.ع موحّدة)، H5 (بوّابة المنيو الفارغ)، H7 (لافتة/سطر انتهاء عرض الإغلاق) | ✅ نُفِّذت — lint/tsc/build/smoke 12/12 |
| **3 — Medium (UI)** | M2 شارة «غير متوفر»، M3 شِبات لاصقة، M4 منتقي كمية، M5 اقتراحات (إصلاح bug الكمية + إبراز الخصم/الشيف)، M6 صقل الترحيب، M7 رفّ شيف مميّز، M9 لافتة offline، M10 skeleton المنيو، M11 fade صور، M12 انتقالات، M13 حركة السلة، M14 padding، M15 عدّ القطع، M16 ckb، M17 H1 المنتج، M18 skip-link، M19 سعر الكابتن | ✅ نُفِّذت — lint/tsc/build/smoke 12/12 · **M8 مؤجَّل** (هوية شاشة المغلق تحتاج backend) |
| **4 — Low (UI)** | L1 (أيقونة السلة الفارغة ShoppingBag)، L2 (رابط مسح السلة من الصفحة بتأكيد خطوتين)، L4 (زرّ مشاركة المنيو — Web Share + نسخ)، L6 (aria-label لشارة الخصم)، L7 (وحدة وقت التحضير كلمة كاملة)، L8 (بطاقات المنيو 4:3)، L9 (تمرير الشِب النشط للعرض)، L12 (`lang="ar"` لشاشة المغلق)، L13 (bdi+lang لخيارات اللغة)، L14 (bdi لاسم المطعم في هيدر المنتج) | ✅ نُفِّذت — lint/tsc نظيف · build OK · smoke 11/12 (`smoke-desync` flake أداء: 1030ms مقابل سقف 1000ms — كل التحققات الوظيفية خضراء) · **L3 (ملاحظة لكل صنف)، L5، L11 مؤجَّلة** (تحتاج backend/نطاق — انظر القسم الأخير) |

## نقاط قوّة (للحفاظ عليها)

- الصور حقيقية وبجودة جيدة عبر `next/image`؛ الشبكة نظيفة ومتّسقة.
- براند المطعم محقون بأمان (لون أساسي/خلفية/بطاقة/نص) وحالة الشِب النشط مزدوجة الإشارة.
- ثلاثية اللغة فعلية مع fallback للعربية؛ RTL/LTR/كردي تُعرض اتجاهيًّا بشكل صحيح في الأغلب.
- وضع الإغلاق واضح: شارة `−10%` حمراء دلالية + سعر مشطوب + سعر جديد (c01/c02/c03).
- «اقرأ للنادل» نصّ كبير مقروء وقائمة مرتّبة (n12).
- حركة دخول شريط السلة + تلاشي تبديل القسم موجودة (أساس جيّد للبناء عليه).

---

# النتائج — High

### H1 · لا يوجد وصف للصنف في صفحة المنتج (فجوة محتوى + بيانات)
- **الشاشة:** المنتج (`n08`, `c02`) · **النوع:** ناقص
- **الملف:** `app/r/[slug]/p/[productId]/product-view.tsx:105-141` · `lib/menu.ts:59-76,158-160`
- **المشكلة:** الصفحة تعرض فقط صورة + اسم + سعر + وقت تحضير + زر إضافة. لا وصف/مكوّنات/حجم حصّة. الجذر بياناتي: `MenuProduct` ولا حتى عمود `description` في الجدول. الزبون يفتح الصنف ليعرف عنه أكثر فلا يجد شيئًا، وأسفل البطاقة فراغ كبير (n08). هذه أعلى فجوة محتوى على السطح كله.
- **التوصية:** أضف عمود `description_ar/en/ku` (اختياري) للمنتجات + `MenuProduct` + الـSELECT، واعرضه تحت الاسم بـ`leading-relaxed` و fallback للعربية مثل `pickName`، مع قصّ سطرين على بطاقة المنيو. **يتطلّب تغيير DB/PRD — راجع `prd.md §4.3` قبل البناء.**

### H2 · لا بحث في المنيو
- **الشاشة:** المنيو (كل الأقسام) · **النوع:** ناقص
- **الملف:** `app/r/[slug]/menu-view.tsx:264-354`
- **المشكلة:** التنقّل عبر شِبات أقسام أفقية فقط؛ لا حقل بحث في أي مكان. مع ١١ قسمًا و٢٣ صنفًا، من يريد «كباب» أو «حمص» يجب أن يمسح الشِبات، يفتح القسم الصحيح، ربما يفتح فرعيًّا، ثم يمسح شبكة. كل منيو QR جادّ (Toast/Square/Flipdish) يوفّر بحثًا فوريًّا، والبيانات محمّلة client-side أصلًا.
- **التوصية:** أضف حقل بحث (في الهيدر أو تحت الشِبات) يفلتر `data.categories[].products` عبر `pickName` (ar/en/ku)، يعرض شبكة نتائج مسطّحة عند وجود استعلام، مع تطبيع همزات/تشكيل العربية، وحالة «لا نتائج» تعيد استخدام `no_menu`.

### H3 · لا تأكيد عند «+ إضافة» (المنيو والمنتج) — أهمّ تفاعل بلا feedback
- **الشاشة:** المنيو (`n03`) + المنتج (`n08`) · **النوع:** ناقص
- **الملف:** `app/r/[slug]/menu-view.tsx:194-197,244-251` · `product-view.tsx:128-140`
- **المشكلة:** `onAdd` ينادي `addToCart`+`track` فقط؛ الردّ الوحيد هو `active:scale` للزرّ الذي يزول فور رفع الإصبع. لا toast، لا «طيران للسلة»، لا نبضة عدّاد، لا `aria-live`. عدّاد السلة في الزاوية المقابلة للإبهام ويتغيّر رقمه فجأة بلا حركة. هذا أكثر فعل تكرارًا في السطح ويبدو كنقرة ميّتة. (يفشل أيضًا SC 4.1.3 لقارئ الشاشة.)
- **التوصية:** عند كل إضافة: (١) نبضة `zoom-in-95` على العدّاد بإعادة تركيبه عبر `key={cartCount}` (`--ease-spring`)، (٢) تبديل أيقونة الزرّ إلى ✓ لـ~600ms، (٣) `navigator.vibrate?.(10)` بحارس، (٤) منطقة `aria-live="polite"` مخفية تعلن «أُضيف {الاسم}». كلها compositor-friendly وتحت `prefers-reduced-motion` (القاعدة العامة تحييدها تلقائيًّا).

### H4 · العملة «IQD» لاتينية بدل «د.ع»، وموضعها غير متّسق بين الشاشات
- **الشاشة:** المنيو/المنتج/السلة (كل سعر) · **النوع:** ناقص + تحسين
- **الملف:** `app/r/[slug]/_ui.tsx:121-157` (PriceTag) مقابل `menu-view.tsx:665-667` (formatPrice)
- **المشكلة:** مُنسِّقان مختلفان: البطاقات/المنتج تعرض «15,000 IQD» (لاحقة)، السلة/«اقرأ للنادل» تعرض «IQD 11,000» (سابقة) — نفس الزبون يرى العملة قبل الرقم في السلة وبعده في المنيو، وفي صفّ c03 الواحد المشطوب لاحقة والإجمالي سابقة (أثر bidi لأن «IQD» لاتيني داخل RTL). والأهم: عرض كود لاتيني «IQD» في واجهة عربية لجمهور عراقي بدل «د.ع» = أبرز إشارة «قالب أجنبي».
- **التوصية:** مُنسِّق نقود واحد `formatMoney(value, currency, lang)`: للعربية/الكردية يعرض «د.ع» بعد الرقم داخل `dir=ltr` span؛ للإنجليزية «IQD». وحّد كل الشاشات عليه. خزّن خريطة الرمز بجوار جدول i18n لتمتدّ للـ١٨ عملة.

### H5 · شاشة «منيو فارغ» تعرض تحية «اختيارات الشيف الليلة» رغم صفر أصناف
- **الشاشة:** منيو فارغ (`e02`) · **النوع:** تحسين
- **الملف:** `app/r/[slug]/menu-view.tsx:255-261`
- **المشكلة:** المقدّمة التحريرية مشروطة بـ`!isOff` فقط، لا بوجود أصناف. مع منيو فارغ يرى الزبون «مساء الخير» + «اختيارات الشيف الليلة» فوق «ما في منيو بعد.» مباشرةً، وشِب «قسم فارغ» أسود غريب (n e02). وعدٌ بـ«اختيارات شيف» فوق منيو فارغ يقرأ كعطل، لا كترحيب.
- **التوصية:** احسب `hasAnyProduct = data.categories.some(c => c.products.length > 0)` واكبت المقدّمة + رفّ الشِبات عندما تكون false، واعرض حالة فراغ واحدة صادقة («المنيو قيد التحضير»). احجز التحية للمنيو الذي به محتوى.

### H6 · خطأ شبكة عابر = نفس شاشة «مغلق» بلا تمييز ولا إعادة محاولة (F5)
- **الشاشة:** مغلق/خطأ (`e01`) · **النوع:** تحسين · **(طبقة بيانات — ليس UI صرف)**
- **الملف:** `lib/menu.ts:124,171` · `app/r/[slug]/page.tsx:13-14`
- **المشكلة:** `loadMenu` يُرجع `null` لثلاث حالات مختلفة دلاليًّا: غير موجود، `is_active=false`، **وفشل قراءة عابر** (`if (catsErr || prodsErr || ...) return null`). كلها تُعرَض كـ`ClosedScreen` «المنيو غير متوفر». زبون يصادف تعثّر Supabase لحظيًّا يُقال له المطعم مغلق نهائيًّا بلا إعادة محاولة — فيستسلم أو يظنّ المطعم (العامل) معطّلًا.
- **التوصية:** اجعل `loadMenu` يميّز الحالات (نتيجة مُميَّزة `{kind:'inactive'|'not_found'|'error'}` أو رمي عند الفشل العابر) ليعرض المسار شاشة خطأ بزرّ «إعادة المحاولة» منفصلة عن شاشة المغلق. **مؤجَّل بطلب المالك (يحتاج backend) — موثّق هنا فقط.**

### H7 · لا عدّاد/إلحاح للزبون في وضع الإغلاق المؤقّت
- **الشاشة:** إغلاق — منيو/منتج/سلة (`c01/c02/c03`) · **النوع:** ناقص
- **الملف:** `app/r/[slug]/p/[productId]/product-view.tsx:102` · `_ui.tsx:103-114`
- **المشكلة:** الإغلاق خصم ينتهي بوقت محدّد، و`closing_mode_ends_at` + `server_now` **موجودان في الـpayload أصلًا** (`lib/menu.ts:91,105`)، لكن الزبون يرى شارة `−10%` ساكنة وسعرًا مشطوبًا فقط. لا شيء يخبره أن العرض مؤقّت أو متى ينتهي — وهو جوهر عرض «happy-hour». بلا إلحاح يقرأ الخصم كسعر دائم، ويُربك حين يعود السعر بصمت بعد المؤقّت (السلة تُعيد التسعير كل ٣٠ث).
- **التوصية:** اعرض شريحة «ينتهي العرض 10:45 م» (من `closing_mode_ends_at` ضدّ `server_now` لتفادي انحراف ساعة العميل) قرب الخصم على المنتج/البطاقات المخصومة/رأس السلة. أضف مفاتيح i18n `offer_ends_at`. أبقها هادئة (لا مؤقّت وامض).

### H8 · أزرار حوار «اطلب من الكابتن» مضلِّلة وخطِرة
- **الشاشة:** اقرأ للنادل (`n12`) · **النوع:** تحسين
- **الملف:** `app/r/[slug]/cart/cart-view.tsx:496-512,464-470`
- **المشكلة:** الزرّ المدمِّر في التذييل يستخدم `t('remove')` → «حذف» لكنه ينادي `onClear` الذي **يُفرِغ السلة كاملة** بلا تأكيد — «حذف» (واحد) تسمية خاطئة وخطِرة لـ«إفراغ الطلب». والزرّ الأساسي يعيد استخدام `t('back_to_menu')` → «العودة للمنيو» لفعل هو فعليًّا «تم/إغلاق». وزرّ الإغلاق (X) أعلى الحوار يحمل نفس `aria-label` «العودة للمنيو» (تصادم تسمية — SC 4.1.2).
- **التوصية:** مفاتيح مخصّصة: `clear_cart` («إفراغ الطلب») **مع خطوة تأكيد** للزرّ المدمِّر، و`done`/`close` («تم»/«إغلاق») للأساسي وزرّ الإغلاق. لا تُعِد استخدام `remove`/`back_to_menu` لأفعال مختلفة دلاليًّا.

### H9 · نص أبيض على لون البراند (الهيدر + زر «+») يفشل تباين WCAG مع ألوان مستأجر فاتحة
- **الشاشة:** هيدر المنتج/السلة + زرّ الإضافة (`n08/n10`) · **النوع:** تحسين (a11y)
- **الملف:** `product-view.tsx:76` · `cart-view.tsx:175` · `menu-view.tsx:596-605`
- **المشكلة:** `color:'#fff'` و`text-white` مثبّتان فوق `primary_color` بلا فحص تباين. مع الزمرّدي `#0f766e` الأبيض ≈ **3.56:1** — تحت 4.5:1 لنصّ الجسم (زرّ الرجوع نصّ وظيفي) وعلى الحدّ لـ3:1 للأيقونة. أي مستأجر أفتح (ذهبي/ليموني/باستيل) يفشل تمامًا. **ملاحظة:** حواجز تباين تبويب التصميم (المرحلة ٢) تفحص text/bg وtext/card وprimary/card — لكنها **لا** تفحص أبيض-على-primary، فهذه فجوة قائمة.
- **التوصية:** استبدل `#fff`/`text-white` بـ`var(--primary-foreground)` (موجود في `globals.css`) أو foreground مشتقّ حسب لمعان OKLCH للـprimary؛ وأضف فحص أبيض-على-primary ≥4.5:1 إلى حارس تبويب التصميم قبل الحفظ.

### H10 · اسم احتياطي عربي يُوسَم بلغة الواجهة → عربي يُعرض LTR (خطأ اتجاه حقيقي)
- **الشاشة:** كل منيو EN/KU به أصناف بلا `name_en/name_ku` (`n05` «حمص») · **النوع:** تحسين (i18n/a11y)
- **الملف:** `lib/i18n.ts:12-19` (pickName) · `app/r/[slug]/_ui.tsx:57-59` (nameLangProps)
- **المشكلة:** `pickName` يرجع `name_ar` احتياطيًّا، لكن الـspan يُوسَم دائمًا بلغة الواجهة المختارة. فعلى منيو EN، اسم عربي احتياطي يُوسَم `lang="en" dir="ltr"` → **نصّ عربي يُعرض LTR** (خطأ بصري)، وعلى KU يُوسَم `ku` (يجب `ckb`). قارئ الشاشة ينطقه بقواعد خاطئة أيضًا.
- **التوصية:** اجعل `pickName` يرجع `{ text, lang }` باللغة التي حُلّت فعلًا، ووسِم الـspan بها لا بلغة الواجهة. يصحّح الاتجاه والنطق معًا.

---

# النتائج — Medium

### M1 · لا وسوم غذائية/حساسية/حار (نباتي، مكسّرات، غلوتين، حار)
- **الشاشة:** بطاقات/منتج · **النوع:** ناقص · **الملف:** `lib/menu.ts:59-76`
- **المشكلة:** لا حقول ولا UI للوسوم. في السوق العربي «حار» و«نباتي» محرّكا قرار شائعان، والحساسية متوقَّعة متزايدًا. زبون بقيد غذائي مضطرّ لسؤال النادل — ما يهزم غاية الخدمة الذاتية.
- **التوصية:** أضف `tags` enum (spicy|vegetarian|vegan|contains_nuts|contains_gluten) اختياري، واعرض رقائق أيقونات صغيرة على البطاقة/المنتج، مع فلترة بالوسم (تتكامل مع البحث H2). ابدأ بـ«حار + نباتي» إن لزم تقليص النطاق.

### M2 · الأصناف غير المتوفرة بلا شارة «غير متوفر» — رمادي فقط
- **الشاشة:** المنيو · **النوع:** ناقص/تحسين · **الملف:** `app/r/[slug]/menu-view.tsx:563-606`
- **المشكلة:** المنتج غير المتاح يُفلتر تمامًا إلا إذا فعّل المطعم `show_unavailable_items`؛ وعند عرضه فقط `opacity-60 grayscale` + زرّ معطّل، **بلا** نصّ «غير متوفر» على البطاقة (المفتاح `unavailable` في i18n موجود لكنه لا يُعرض إلا في صفحة المنتج). صورة رمادية تقرأ كصورة باهتة لا كـ«نفد».
- **التوصية:** اعرض شارة/overlay «غير متوفر» على صورة البطاقة (نمط DiscountBadge برمز محايد/destructive)، وأضف `aria-label` للزرّ المعطّل يوضّح السبب.

### M3 · لا فهرس أقسام/قفز، والشِبات لا تُثبَّت ولا تتبع التمرير
- **الشاشة:** المنيو (`n07`) · **النوع:** ناقص · **الملف:** `app/r/[slug]/menu-view.tsx:263-294`
- **المشكلة:** الشِب فلتر: نقره يستبدل الشبكة بقسم واحد. لا منيو واحد طويل برؤوس أقسام لاصقة وفهرس يبرز القسم الحالي. الهيدر اللاصق يبقي البراند/اللغة/السلة لكن **شِبات الأقسام غير مثبّتة فيه**، فمن يمرّر قسمًا طويلًا يفقد الشِبات ويجب العودة للأعلى للتبديل.
- **التوصية:** ثبّت رفّ الشِبات داخل الهيدر اللاصق مع تحديث الشِب النشط عبر `IntersectionObserver`؛ أو انتقل لمنيو طويل واحد برؤوس لاصقة + scroll-spy. الخيار الأول أصغر ويحلّ «فقدان الشِبات أثناء التمرير».

### M4 · لا منتقي كمية في صفحة المنتج
- **الشاشة:** المنتج · **النوع:** ناقص · **الملف:** `app/r/[slug]/p/[productId]/product-view.tsx:128-140`
- **المشكلة:** زرّ الإضافة يضيف ١ دائمًا؛ لطلب ٣ يجب الإضافة ثم الذهاب للسلة والنقر ٣ مرّات. صفحة المنتج بها فراغ كبير يتّسع لمنتقي كمية (توقّع قياسي).
- **التوصية:** أضف منتقي كمية (أعد استخدام نمط `CarRow` +/−) ومرّر الكمية لـ`addToCart`؛ الافتراضي ١.

### M5 · الاقتراحات ضعيفة — بلا «سبب»، لا تُبرز الخصم، و bug: نقر اقتراح يصفّر الكمية إلى ١
- **الشاشة:** السلة (`n10/c03`) · **النوع:** تحسين · **الملف:** `app/r/[slug]/cart/cart-view.tsx:359-396`
- **المشكلة:** خوارزمية الثلاث خطوات سليمة لكن المخرَج ضعيف: البطاقة صورة+اسم+سعر بلا «لماذا» («يُطلب مع الحمص»/«اختيار الشيف»/«عرض»). خطوة «العشوائي» ليست عشوائية (نفس العناصر دائمًا)، وفي وضع الإغلاق (c03) الاقتراحات تعرض السعر الكامل بلا شارة خصم. والأهم: النقر ينادي `setQuantity(slug, id, 1)` فيُعيد كمية صنف موجود إلى ١ (**فقدان بيانات عند نقرة خاطئة** + بلا feedback).
- **التوصية:** أضف شارة سبب لكل اقتراح، أبرز الخصم على المخصوم، وأولويّة المخصومات في وضع الإغلاق؛ غيّر فعل النقر إلى `current+1` مع تأكيد؛ وخلط حقيقي لخطوة العشوائي.

### M6 · شاشة الترحيب فارغة وانطباع أول ضعيف + tagline عام (ليس صوت المطعم)
- **الشاشة:** الترحيب (`n02b`) · **النوع:** تحسين · **الملف:** `app/r/[slug]/welcome-screen.tsx:104-131,124-130`
- **المشكلة:** شعار + اسم + تحية + سطر واحد، متمركزة بـ`flex-1`، فيبقى ~٤٠-٥٠٪ من الشاشة فراغًا. لا صورة طعام، لا نوع مطبخ، لا حالة «مفتوح الآن». الـtagline «منيو الليلة جاهز، اختر بروح الشيف...» مثبّت لكل مطعم (ليس صوته) ويشير لـ«الشيف» ما يضلّل في وضعَي الإغلاق/الإيقاف. سطح مسطّح بلا عمق/نسيج.
- **التوصية:** خلفية برّاند متدرّجة/نسيج خفيف + معالجة شعار أكبر، وسطر اختياري للمطعم (tagline من DB) مع fallback، وربما شريط صور «اختيارات الليلة»؛ اضبط الإيقاف العمودي قرب الثلث البصري. افصل نصّ الـtagline عن إطار «الشيف».

### M7 · رفّ «اختيارات الشيف» مطابق بصريًّا للشبكة العادية
- **الشاشة:** المنيو (`n03/n04`) · **النوع:** تحسين · **الملف:** `app/r/[slug]/menu-view.tsx:296-323`
- **المشكلة:** الرفّ يعيد استخدام نفس `ProductCard` (نفس البطاقة البيضاء/المربّع/زر +/السعر)؛ التمييز الوحيد شريط 5px + عنوان. إشارة «المنسَّق/المميّز» غائبة — لا يميّز الزبون الرفّ عن باقي المنيو.
- **التوصية:** اجعل الرفّ بطاقات أكبر بنِسبة landscape، الاسم/السعر فوق صورة معتمة، سطح/حدّ برّاند، شارة «اختيار الشيف» ⭐، فيقرأ كـcarousel توصية لا شبكة مكرّرة.

### M8 · شاشة «مغلق» بلا هوية/شعار/ساعات/إعادة محاولة
- **الشاشة:** مغلق (`e01`) · **النوع:** تحسين · **الملف:** `app/r/[slug]/closed-screen.tsx:7-22`
- **المشكلة:** بطاقة عامة (فنجان قهوة + «المنيو غير متوفر») بلا اسم/شعار/لون/ساعات/إجراء. الزبون لا يتأكّد أنه وصل المطعم الصحيح، ولا متى يفتح، ولا فعل. الـslug **معروف** في `page.tsx` لكن `loadMenu` أهمل صفّ المطعم بإرجاع null للحالة المعطّلة.
- **التوصية:** لحالة المعطّل (لا الخطأ) أرجِع هوية مبسّطة من `loadMenu` رغم `is_active=false` (الاسم/الشعار/اللون/ساعات اختيارية) واعرضها + زرّ «حاول مرة أخرى». بدّل فنجان القهوة برمز «ساعة/إيقاف مؤقّت».

### M9 · لا مؤشّر «أنت غير متصل» عند خدمة المنيو من الكاش
- **الشاشة:** offline · **النوع:** ناقص · **الملف:** `public/sw.js` · `app/r/[slug]/menu-view.tsx:100`
- **المشكلة:** الـSW يخدم آخر HTML/`/api/menu` مكاشَين عند انقطاع الشبكة (يعمل في الإنتاج — مؤكَّد بـ`smoke-pwa`)، لكن **لا شيء** يخبر الزبون أنه offline أو أن الأسعار/التوفّر قد تكون قديمة. عرض الإغلاق قد يكون انتهى، أو صنف نفد، بلا إشارة. الـpoll كل ٣٠ث يفشل بصمت (`catch {}`).
- **التوصية:** شريط offline عبر `useSyncExternalStore` على أحداث `online`/`offline` (موثّق في `react/hooks.md`): «أنت غير متصل — نعرض نسخة محفوظة من المنيو»، يُمسح عند العودة. ثلاثي اللغة.

### M10 · skeletons التحميل لا تطابق الوجهة (قفزة تخطيط)
- **الشاشة:** loading · **النوع:** تحسين · **الملف:** `app/r/[slug]/loading.tsx:3-13`
- **المشكلة:** الـskeleton عنقود متمركز (دائرة + سطران) لا يشبه لا شاشة الترحيب (شِب لغة أعلى + CTA أسفل) ولا المنيو (هيدر + شِبات + شبكة). فعند ظهور المحتوى يقفز من وسط الشاشة إلى تخطيط مثبّت أعلى = CLS وإحساس غير مصقول.
- **التوصية:** اجعل الـskeleton يحاكي أول رسم فعلي (نمذجة على الترحيب: شِب لغة + شعار/عنوان متمركز + شريط CTA سفلي)، بأبعاد كتل مساوية للعناصر الحقيقية لتفادي القفزة.

### M11 · الصور تظهر فجأة بلا fade/blur-up
- **الشاشة:** كل الشاشات · **النوع:** تحسين · **الملف:** `app/r/[slug]/_ui.tsx:81`
- **المشكلة:** `MenuImage` لا يملك `onLoad` fade ولا `blurDataURL` ولا shimmer؛ كل صورة تقطع فجأة من صندوق رمادي إلى صورة كاملة. مع ٢٣ صورة (n03) + hero (n08) على شبكة مطعم، هذا أبرز ضعف أداء محسوس.
- **التوصية:** تتبّع `onLoadingComplete` وانتقل opacity 0→1 بـ`--ease-out-expo`، مع `placeholder="blur"` (blurDataURL من خط الصور). أبقِ احتياطي `UtensilsCrossed`. مستخدمو reduced-motion يحصلون على التبديل الفوري.

### M12 · انتقال الترحيب→المنيو قطع حادّ + شريط السلة بلا حركة خروج
- **الشاشة:** ترحيب→منيو (`n03`) · **النوع:** تحسين (حركة) · **الملف:** `app/r/[slug]/menu-view.tsx:208,625`
- **المشكلة:** `started=true` يستبدل `<WelcomeScreen>` بالمنيو في إطار واحد — اللحظة البرّاندية الوحيدة تنتهي بقطع. وشريط السلة يدخل بحركة لكنه `return null` عند الإفراغ (دخول ناعم، خروج حادّ = خلل). و`FloatingCart` (المنتج) بلا دخول ولا خروج (سلوك غير متّسق بين الشاشتين).
- **التوصية:** غلّف جذر المنيو بـ`animate-in fade-in slide-in-from-bottom-2` عند قلب `started`؛ وقُد ظهور/اختفاء شريط السلة بانتقال خروج (إبقاء العقدة لحظة + `animate-out`) ووحّد نفس المفردات مع `FloatingCart`.

### M13 · وحدات الكمية/الإجمالي بلا حركة، وإزالة الصف فورية
- **الشاشة:** السلة (`n10`) · **النوع:** تحسين (حركة) · **الملف:** `app/r/[slug]/cart/cart-view.tsx:320,216,341`
- **المشكلة:** أزرار +/− والإجمالي تتغيّر بتبديل رقم حادّ بلا easing؛ إزالة صفّ تحذف `<li>` فورًا فتقفز القائمة. لحلقة «عدّل طلبي» الأساسية يبدو ميكانيكيًّا، وأزرار الكمية بلا feedback ضغط (`hover` فقط).
- **التوصية:** أضف `active:scale-90` لأزرار +/−، ووميض 200ms للإجمالي المتغيّر، وطيّ height/opacity عند إزالة الصف — كلها تحت `--animate-duration-fast` وآمنة لـreduced-motion.

### M14 · زرّ «اطلب من الكابتن» العائم يغطّي الاقتراحات
- **الشاشة:** السلة (`n10`) · **النوع:** تحسين · **الملف:** `app/r/[slug]/cart/cart-view.tsx:188-262`
- **المشكلة:** الزرّ الثابت المتمركز `bottom-4` يغطّي آخر صفّ اقتراحات (يبدو تصادم z-index لا فعلًا عائمًا مصمَّمًا) رغم `pb-32`.
- **التوصية:** زِد padding سفل الحاوية (`pb-40` + safe-area) أو ثبّت الـCTA كشريط سفلي صلب بسطح/حدّ خاص، فلا يُخفى أي اقتراح.

### M15 · «اقرأ للنادل»: لا عدّ أصناف ولا مسار «طلب جديد/تم»
- **الشاشة:** اقرأ للنادل (`n12`) · **النوع:** ناقص · **الملف:** `app/r/[slug]/cart/cart-view.tsx:473-495`
- **المشكلة:** الحوار يسرد أصنافًا وإجماليًّا لكن لا يذكر العدد («٣ أصناف / ٥ قطع») المفيد للكابتن. ولا مسار واضح «طلبت → ابدأ طلبًا جديدًا»؛ المخرَج الوحيد المنطقي مدفون خلف «حذف» الأحمر (انظر H8).
- **التوصية:** اعرض عدّ أصناف/قطع قرب الإجمالي (`rows.length` / `sum(quantity)`)، وأعد صياغة التذييل كـ«تم الطلب — طلب جديد» (إفراغ + إغلاق) متميّزًا عن «إغلاق» هادئ.

### M16 · وسم اللغة الكردية غير متّسق (`ku` مقابل `ckb`)
- **الشاشة:** كل سطوح KU (`n05`) · **النوع:** تحسين (a11y) · **الملف:** `app/r/[slug]/_ui.tsx:42,57-58` · `lib/i18n.ts`
- **المشكلة:** النصّ سوراني بخط عربي. قائمة اللغة تَسِم `lang="ckb"` بشكل صحيح، لكن `useSyncHtmlLang` يضع `lang="ku"` و`nameLangProps` يرجع `ku`. فالمحتوى ذاته يُوسَم بطريقتين، و`ku` لا يخبر قارئ الشاشة/مشكِّل الخط أنه سوراني-عربي.
- **التوصية:** وحّد على `ckb` (أو `ckb-Arab`) لسمة BCP-47 في كل مكان (`document.documentElement.lang` + `nameLangProps`) مع إبقاء مفتاح `Lang` الداخلي `ku` عبر خريطة.

### M17 · H1 في صفحة المنتج = اسم المطعم، لا المنتج (هرمية خاطئة)
- **الشاشة:** المنتج · **النوع:** تحسين (a11y) · **الملف:** `app/r/[slug]/p/[productId]/product-view.tsx:86-106`
- **المشكلة:** الهيدر `<h1>{display_name}</h1>` واسم المنتج `<h2>`. مستخدم قارئ الشاشة يصل «origins» كـH1 ثم «چاي» كـH2 — المنتج تابع للمطعم في شجرة العناوين (SC 1.3.1).
- **التوصية:** اخفض اسم المطعم في الهيدر إلى `<p aria-label>` (وظيفته بصرية/تنقّل)، وارفع اسم المنتج إلى `<h1>`.

### M18 · لا رابط «تخطَّ إلى المحتوى» (Bypass Blocks)
- **الشاشة:** كل الشاشات · **النوع:** ناقص (a11y) · **الملف:** `app/r/[slug]/menu-view.tsx:224-254`
- **المشكلة:** هيدر لاصق (براند + لغة + سلة) + صفّا شِبات قبل المحتوى، بلا رابط تخطٍّ لمستخدمي الكيبورد/التبديل (SC 2.4.1 A).
- **التوصية:** أضف `<a href="#menu-content" className="sr-only focus:not-sr-only ...">تخطَّ إلى المحتوى</a>` كأول عنصر، و`id="menu-content"` على `<section>` الشبكة. كرّره على المنتج والسلة.

### M19 · سعر الكابتن muted وصغير لقراءة عبر الطاولة
- **الشاشة:** اقرأ للنادل (`n12`) · **النوع:** تحسين (a11y) · **الملف:** `app/r/[slug]/cart/cart-view.tsx:483-487`
- **المشكلة:** الحوار مُصمَّم للعرض/القراءة للنادل، لكن إجمالي السطر `text-base text-muted-foreground` (~5.5:1، يمرّ تقنيًّا لكن هامشي عبر الطاولة في إضاءة خافتة)، بينما الاسم `text-2xl bold`. التباين بالحجم/الوزن يُجبر النادل على الانحناء.
- **التوصية:** ارفع سعر السطر إلى `text-lg font-bold` و`text-foreground` (تباين كامل)، كما في صفّ الإجمالي.

---

# النتائج — Low

### L1 · سلة فارغة بأيقونة مكبّر صوت (دلالة خاطئة)
- المنتج/النوع: سلة فارغة (`n13`) · تحسين · `app/r/[slug]/cart/cart-view.tsx:194`
- استخدام `Megaphone` (رمز «اقرأ للنادل») فوق «سلتك فارغة» مربك. **التوصية:** استخدم `ShoppingBag` (مطابق لأيقونة السلة بالهيدر)، ورقِّ رابط «العودة للمنيو» إلى زرّ مملوء.

### L2 · لا «إفراغ السلة» في صفحة السلة نفسها
- السلة (`n10`) · ناقص · `app/r/[slug]/cart/cart-view.tsx:221-228`
- الإفراغ ممكن فقط داخل حوار النادل (ومسمّى خطأً). على صفحة السلة يجب حذف كل صفّ يدويًّا. **التوصية:** أضف «إفراغ السلة» (مع تأكيد) قرب الإجمالي، يعيد استخدام مفتاح `clear_cart`.

### L3 · لا ملاحظة لكل صنف (بدون بصل/حار) — ضمن النطاق
- السلة/المنتج · ناقص · `app/r/[slug]/cart/cart-view.tsx:282-357`
- لا حقل ملاحظة قصير ليُعرَض على شاشة الكابتن. لمنيو يُقدَّم نفسه كـ«اعرضه/اقرأه للنادل»، تعذّر تمرير تعديلات بسيطة فجوة بارزة (حقل ملاحظة، ليس طلبًا أونلاين). **التوصية:** ملاحظة نصّية اختيارية لكل سطر في cart localStorage، تُعرَض في `CartRow` و`ReadToWaiterModal`. **راجع نطاق PRD أولًا (يمسّ شكل السلة).**

### L4 · لا مشاركة المنيو
- الهيدر · ناقص · `app/r/[slug]/menu-view.tsx:227-253`
- لا `navigator.share`/نسخ رابط لمشاركة `/r/{slug}` مع بقية الطاولة (نمط شائع، رخيص على PWA). **التوصية:** زرّ مشاركة صغير في الهيدر يستدعي `navigator.share` مع fallback نسخ.

### L5 · لا «نداء الكابتن» من المنيو
- المنيو/السلة · ناقص (مجاور للنطاق) · `app/r/[slug]/menu-view.tsx:224-253`
- الوحيد هو «اطلب من الكابتن» السلبي في السلة. **التوصية:** اعرض المفهوم من المنيو أيضًا، أو رابط `tel:` إن كان للمطعم هاتف. **تحقّق من نطاق PRD قبل أي إشارة نداء حيّة.**

### L6 · شارة الخصم بلا `aria-label` (نطق غير متّسق)
- إغلاق · تحسين (a11y) · `app/r/[slug]/_ui.tsx:103-113`
- `−{percent}%` بعلامة U+2212 يُنطَق بطرق مختلفة عبر القُرّاء. **التوصية:** `aria-label={`${t('discount_off', lang)} ${percent}%`}` («خصم 10%») — المفتاح `discount_off` موجود.

### L7 · وحدة وقت التحضير «د» مبهمة (تصادم مع «د.ع») وبلا تسمية
- المنتج (`n08/n09`) · تحسين · `app/r/[slug]/p/[productId]/product-view.tsx:115-120` · `lib/i18n.ts:48`
- «د» (دقيقة) يلتبس بـ«دينار» — خصوصًا بعد توطين العملة لـ«د.ع» (H4). **التوصية:** اكتب الوحدة كاملة («دقيقة»/«min»/«خولەک») أو أضف تسمية مرئية.

### L8 · القصّ المربّع الموحّد يسطّح المنيو ويبتر الأطباق
- المنيو (`n03/n04`) · تحسين · `app/r/[slug]/menu-view.tsx:336-352` · `_ui.tsx:81-99`
- `aspect-square object-cover` يقصّ الأطباق الممدودة (السيخ/الكباب) ويعطي شبكة رتيبة موحّدة (النمط المحظور في design-quality). **التوصية:** نِسبة 4:3 لتأطير أفضل، وربما خلية «feature/bento» للعنصر الأول/اختيار شيف لكسر الرتابة.

### L9 · الشِب النشط قد يبقى مقصوصًا (لا `scrollIntoView`)
- المنيو (`n03`) · تحسين · `app/r/[slug]/menu-view.tsx:515,186-190`
- شِب مختار في الطرف قد يبقى خارج الشاشة. **التوصية:** `el.scrollIntoView({ inline:'center', behavior: reduced?'auto':'smooth' })` داخل `pickParent`.

### L10 · تبديل القسم تلاشٍ مسطّح بلا اتجاه
- المنيو · تحسين (حركة) · `app/r/[slug]/menu-view.tsx:336`
- `fade-in-0` فقط — التبديل بين الأقسام يبدو كرمشة بلا إحساس حركة. **التوصية:** انزلاق اتجاهي قصير (`slide-in-from-bottom-2`) ضمن `--animate-duration-fast`.

### L11 · لا View Transitions بين الشاشات
- كل التنقّلات · تحسين (حركة) · `app/r/[slug]/menu-view.tsx` (التنقّل)
- التنقّل منيو→منتج→سلة قطع حادّ (الـskeletons للتحميل البارد فقط). **التوصية:** View Transitions API (أو `view-transition-name` على صورة المنتج المشتركة بين البطاقة والـhero) — أعلى مكسب أداء محسوس، يعيد استخدام `--ease-out-expo`.

### L12 · `ClosedScreen` بلا سمة `lang`
- مغلق (`e01`) · تحسين (a11y) · `app/r/[slug]/closed-screen.tsx:9`
- `<main dir="rtl">` بنصّ عربي بلا `lang` (SC 3.1.1). **التوصية:** أضف `lang="ar"` صراحةً (مستقلًّا عن الجذر).

### L13 · أزرار لغة popup الترحيب بلا `lang/dir`
- الترحيب (`n01`) · تحسين (a11y) · `app/r/[slug]/welcome-screen.tsx:165-179`
- قائمة المنيو تَسِم كل خيار `lang/dir`؛ popup الترحيب لا (SC 3.1.2). **التوصية:** غلّف كل تسمية بـ`<span lang={l.code==='ku'?'ckb':l.code} dir={isRtl(l.code)?'rtl':'ltr'}>` (الدالة `isRtl` مستوردة أصلًا).

### L14 · اسم مطعم لاتيني بلا `bdi` في هيدر RTL
- الهيدر (`n08/n09`) · تحسين (a11y) · `product-view.tsx:86` · `menu-view.tsx` BrandMark · `cart-view.tsx:185`
- اسم لاتيني/مختلط («origins») في سطر RTL يعتمد على bidi بلا عزل. **التوصية:** غلّف `display_name` بـ`<bdi>` (أو `dir="auto"`) لعزله عن العناصر العربية المجاورة.

---

## ملاحظات تغطية وقيود الالتقاط

- **offline + skeletons** لم تُلتقطا بصريًّا (انظر أعلى)؛ غُطّيتا كوديًّا/سلوكيًّا (M9, M10) — `smoke-pwa` يؤكّد أن offline يعمل في الإنتاج.
- **منيو KU** التُقط بنجاح بعد تصحيح نصّ زرّ «مینۆ بکەرەوە»؛ **حوار «اطلب من الكابتن»** بعد تصحيح النصّ («اطلب من الكابتن» لا «اقرأ للنادل»).
- لم تُختبَر متصفّحات متعدّدة ولا أجهزة فعلية (Playwright/Chromium فقط) — يُنصح بفحص iOS Safari يدويًّا (خصوصًا PWA/offline والـ`100dvh` لحوار النادل).

## مؤجَّل (يحتاج تغيير طبقة بيانات/PRD — ليس UI صرفًا)
- **H1** (عمود وصف المنتج) · **H6 / F5** (تمييز «مغلق» عن «خطأ شبكة») · **M1** (وسوم غذائية) · **L3** (ملاحظة لكل صنف) — كلها تمسّ المخطّط/الـPRD؛ تُراجَع مع `prd.md` قبل البناء.

## ملخّص العدد
**High: 10 · Medium: 19 · Low: 14** (مُجمَّعة من ٦٧ نتيجة خام عبر ٧ عدسات، بعد دمج التكرارات وإسقاط ما هو خارج النطاق).
