# OWNER-PANEL-PLAN.md

**المرحلة 1 — تدقيق + خطة (تقرير فقط).** لا كود، لا migrations في هذه المرحلة.
كل مقترحات المخطط أدناه موسومة **«مقترح — لا يُطبّق الآن»**.

- التاريخ: 2026-06-05
- النطاق: توسيع لوحة الأونر `/owner` لتغطية إدارة كل المطاعم + الفوترة + تحليلات الإيراد + سجل تدقيق.
- المراجع: `prd.md` (§3.1 التصميم، §4.3 المخطط، §4.4 RLS، §8 خارج النطاق)، `CLAUDE.md`، `RULES.md`.

### ✅ قرارات Mustafa المحسومة (2026-06-05)
1. **نموذج الفوترة: المسار الهجين C** — جدول `payments` مرن، بلا محرّك اشتراكات منفصل.
2. **الفروع: عدّاد تسعير فقط (i)** — `branch_count` للتسعير، بلا وظائف multi-branch. **متوافق مع §8.**
3. **عملة التحليلات: تجميع لكل عملة** — بلا أسعار صرف، مع إبراز IQD.

**ما زال مفتوحاً** (قرارات 4–6 في القسم 8): سلوك الحذف (soft-delete)، تعريف «آخر نشاط»، وهل نضيف لـ`prd.md`/`RULES.md` سطراً يوثّق أن «تسجيل الدفعات (دفتر offline) + تجديد اختياري» أصبح ضمن النطاق — لأن النصّ الحالي يقول الدفع offline بلا تتبّع.

---

## 0) ملخّص تنفيذي

لوحة الأونر الحالية تغطّي **شريحة ضيقة فقط**: نظرة عامة بأربع بطاقات عدّ، وإدارة حسابات أساسية (إنشاء / تفعيل-تعطيل / تغيير كلمة سر / حذف). البنية الأمنية سليمة وقوية (`requireOwner` على كل فعل، proxy + RLS). أما **الفوترة والإيراد والخطط والتجديدات وسجل التدقيق فغير موجودة إطلاقاً** — لا جداول، لا حقول، لا واجهات.

أكبر عائق قبل أي بناء هو **تعارض مفاهيمي يجب أن تحسمه أنت** (القسم 3): النموذج التجاري الموثّق في `CLAUDE.md`/PRD هو **«شراء لمرة واحدة، بلا اشتراكات، فرع واحد»**، بينما طلبك يفترض **خطط + تجديدات + فروع**. تحليلات الإيراد كلها تتفرّع من هذا القرار.

---

## 1) تدقيق الوضع الحالي

### 1.1 لوحة `/owner` — الموجود فعلاً

| المسار | الملف | الوظيفة |
|---|---|---|
| `/owner` | `app/owner/page.tsx` + `login-form.tsx` + `actions.ts` | تسجيل دخول الأونر (Supabase Auth + فحص `app_metadata.role === 'owner'`) |
| `/owner/dashboard` | `app/owner/dashboard/page.tsx` | نظرة عامة: 4 بطاقات (إجمالي/نشط/معطّل/منتجات) + آخر 5 حسابات |
| `/owner/dashboard/accounts` | `accounts/page.tsx` + `accounts-table.tsx` + `actions.ts` | جدول الحسابات + بحث + إنشاء/تفعيل/كلمة سر/حذف |
| layout | `dashboard/layout.tsx` + `owner-nav-link.tsx` | رأس بتبويبين فقط: «نظرة عامة» و«الحسابات» |

**الأفعال الموجودة** (`accounts/actions.ts`، كلها خلف `requireOwner`):
- `createAccount` — يزوّد: `display_name`, `slug` (فريد), `username` (فريد), `password` (≥8، bcrypt). **لا يضبط**: لغات، عملة، خطة، سعر، فروع.
- `setAccountActive(id, bool)` — يقلب `is_active`.
- `changeAccountPassword(id, pw)` — يحدّث الهاش + **يُبطل كل جلسات المستأجر** (إجبار إعادة دخول).
- `deleteAccount(id)` — **حذف صلب (hard delete)**: يمسح صور R2 ثم يحذف صف المطعم (cascade على كل شيء).

**ما تعرضه قائمة الحسابات حالياً**: الاسم، `/r/{slug}`، `username`، الحالة (Switch)، عدد المنتجات + الأصناف، آخر دخول (`last_login_at`). بحث على (الاسم/slug/username). **لا فرز، لا خطة، لا تجديد، لا فروع، لا تفاصيل مطعم، لا تعديل بيانات**.

### 1.2 المخطط الحالي (Supabase Postgres) — `restaurants` وحقوله

مجمّعة من `0001_init` + `0006_off_mode` + `0007_design_colors`:

| الفئة | الأعمدة الموجودة |
|---|---|
| هوية | `id`, `slug` (unique), `display_name`, `username` (unique), `password_hash`, `is_active` |
| تصميم | `logo_url`, `primary_color`, `background_color`, `currency` (افتراضي IQD), `header_color`, `card_color`, `text_color` |
| إعدادات | `show_unavailable_items` |
| الأوضاع | `active_mode` ∈ `{normal, closing, off}`, `closing_mode_ends_at`, `closing_mode_discount` |
| ميتا | `created_at`, `last_login_at` |

**الجداول الأخرى**: `categories`, `products`, `complementary_categories`, `tenant_sessions` (بلا `expires_at` — دائمة)، `events` (تتبّع تفاعل الزبون فقط: `menu_open/product_open/product_add` — **ليست مبيعات**).

**العملات**: `lib/currencies.ts` يعرّف 18 عملة مدعومة (IQD أولاً) — جاهزة للاستخدام في الفوترة.

### 1.3 المصادقة والأمان — قويّ ولا يُمسّ

- `lib/auth/require-owner.ts`: يتحقّق من `app_metadata.role === 'owner'` عبر `getUser()` ويعيد التوجيه لـ`/owner` وإلا. **مكتوب كأول سطر في كل فعل/محمّل أونر** (SECURITY-FINDINGS C-1: الأفعال تُستدعى عبر `Next-Action` خارج الـproxy، فالـproxy وحده غير كافٍ).
- `proxy.ts`: يبوّب `/owner/dashboard/*` بـ`getUser()` فعلياً (إنفاذ الجلسات المُبطلة).
- RLS: «Owner full access» على كل الجداول عبر `auth.jwt() role = owner`. القراءات العامة مقيّدة بـ`is_active = TRUE`. عزل المستأجر يمرّ عبر service-role في API، لا عبر RLS.

### 1.4 ما هو موجود vs ناقص (جدول سريع)

| المطلوب | الحالة |
|---|---|
| جدول مطاعم + حقول أساسية | ✅ موجود |
| إنشاء/تفعيل/حذف حساب | ✅ موجود (حذف صلب) |
| تغيير كلمة سر مع إبطال الجلسات | ✅ موجود |
| بحث في الحسابات | ✅ (بحث فقط، **لا فرز**) |
| تزويد لغات/عملة عند الإنشاء | ❌ ناقص |
| خطة + سعر + فروع | ❌ غير موجود (لا حقل) |
| عرض تفاصيل مطعم (محتواه/إعداداته/حساباته) | ❌ ناقص |
| تعديل بيانات أي مطعم | ❌ ناقص |
| Soft-delete | ❌ (الحذف صلب حالياً) |
| **تتبّع دفعات/اشتراكات** | ❌ **غير موجود إطلاقاً** |
| تحليلات إيراد | ❌ غير موجود |
| تجديدات قادمة / متأخرات | ❌ غير موجود |
| سجل تدقيق (من غيّر ماذا ومتى) | ❌ غير موجود |
| `requireOwner` على كل إجراء | ✅ نمط قائم يجب أن يستمر |

---

## 2) تحقّق الفوترة

**النتيجة: لا يوجد أي تتبّع للدفعات أو الاشتراكات — كما توقّعت.**

- لا جدول `payments` / `subscriptions` / `invoices`.
- لا حقول `plan` / `price` / `paid_at` / `renewal` / `status` على `restaurants`.
- `events` يتتبّع تفاعل الزبون فقط، لا المال.
- الدفع يُدار **offline يدوياً** (CLAUDE.md: «Mustafa creates tenant accounts manually; payments are handled offline»). لا يوجد حتى سجل لما دُفع.

⇒ **تحليلات الإيراد مستحيلة بلا نموذج بيانات فوترة جديد.** هذا هو القرار المركزي (القسم 3 + 6).

---

## 3) ⚠️ تعارض يجب حسمه قبل البناء — النموذج التجاري

`CLAUDE.md`/PRD يوثّقان النموذج صراحةً:

> «one-time-purchase SaaS **(no subscriptions)** for small/medium **single-branch** restaurants»
> §8 خارج النطاق يشمل: **online payments، multi-branch، loyalty/coupons**.

طلبك للمرحلة يفترض عكس ذلك في ثلاث نقاط:

| طلبك | الموثّق حالياً | نوع التعارض |
|---|---|---|
| خطة + **تجديد** + «اشتراك» (payments/**subscriptions**) | «شراء لمرة واحدة، بلا اشتراكات» | جوهري |
| **فروع** (حقل + تفصيل تحليلات حسب الفروع) | «فرع واحد»، multi-branch خارج النطاق | جوهري |
| تسجيل دفعات | الدفع offline | **متوافق** — تسجيل ≠ معالجة |

**نقطة مهمّة لصالحك**: تسجيل دفعة يدوية **لا يخالف** «online payments خارج النطاق». أنت لا تبني بوّابة دفع؛ تبني **دفتر حسابات** لما تستلمه يدوياً. هذا آمن.

لكن **«اشتراكات/تجديد» و«فروع» يخالفان النموذج الموثّق**. حسب انضباط `CLAUDE.md` («If something contradicts RULES.md, stop and surface it») أطرح ثلاثة مسارات — **اختر واحداً قبل أن أبني**:

- **المسار A — شراء لمرة واحدة (يلتزم بالـPRD).**
  دفعة واحدة = ترخيص. لا محرّك اشتراكات. «التجديد» اختياري بصيغة **ترخيص سنوي / تجديد دعم** يدويّ. تحليلات الإيراد = تأسيس + تجديدات يدوية. **أقل تعارض، أبسط بناء.**

- **المسار B — اشتراكات كاملة (يغادر الـPRD).**
  جدول `subscriptions` بدورة + تاريخ تجديد + حالة. يتطلّب **تحديث `RULES.md` و`prd.md` §8 رسمياً** أولاً. أكثر مرونة، لكنه قرار منتج كبير.

- **المسار C — هجين.**
  جدول `payments` مرن يقبل دفعة لمرة واحدة **و** دفعة دورية، مع حقل فترة اختياري. يغطّي الحالتين دون التزام بمحرّك اشتراكات كامل. **توصيتي المبدئية** (القسم 6 مصمَّم عليه) لأنه يسجّل الواقع الحالي (دفعات يدوية متفرّقة) ويترك باب التجديد مفتوحاً بلا فرض.

**وبشأن «الفروع»**: هل تقصد
- (i) **عدّاد فروع للتسعير فقط** (مطعم بفرعين يُسعَّر أعلى) — مجرّد رقم `branch_count` على الفوترة، **بلا** بناء وظائف multi-branch؟ ← متوافق ومقبول. أو
- (ii) **multi-branch فعلي** (قوائم/حسابات منفصلة لكل فرع)؟ ← يخالف §8 صراحةً وأرفض بناءه دون تحديث الـPRD.

أرجّح أنك تقصد (i). أكّد لي.

---

## 4) الفجوة التفصيلية: الموجود مقابل المطلوب

### أ) إدارة المطاعم

| البند | الحالة | الفجوة / المطلوب |
|---|---|---|
| قائمة كل المطاعم | جزئي | إضافة أعمدة: **خطة، فروع، تاريخ الانضمام (موجود: `created_at`)، التجديد القادم، آخر نشاط**. «آخر نشاط» = `max(events.created_at)` (نشاط الزبون) أو `last_login_at` (دخول الأدمن) — أرى عرض الاثنين. |
| بحث/فرز | بحث فقط | إضافة **فرز** على كل عمود (الاسم/التاريخ/الحالة/الخطة/التجديد). |
| إضافة مطعم جديد | جزئي | توسيع التزويد ليشمل: **لغات (ar إلزامي، en/ku اختياري)، عملة (من 18)، خطة + سعر + فروع، حساب أدمن أولي** (موجود: username/password). إنشاء صف الفوترة الأول ذرّياً مع المطعم. |
| عرض تفاصيل مطعم | مفقود | صفحة `/owner/dashboard/restaurants/[id]`: أصنافه + فئاته + إعداداته + حساباته/جلساته + نشاطه + سجل دفعاته. |
| تعديل بيانات أي مطعم | مفقود | فعل `updateRestaurant` (اسم، slug مع فحص تفرّد، لغات، عملة، خطة/سعر/فروع، ألوان). |
| تعليق/تفعيل | ✅ موجود | يبقى (`setAccountActive`). |
| حذف | صلب | **التحويل إلى soft-delete** (`deleted_at`) مع تأكيد؛ إبقاء الحذف الصلب كإجراء أونر منفصل «حذف نهائي + R2 purge». |

### ب) الفوترة

| البند | الحالة | المطلوب |
|---|---|---|
| تسجيل دفعة | مفقود | فعل `recordPayment` يكتب في جدول `payments` المقترح (القسم 6). |
| سجل الدفعات | مفقود | عرض لكل مطعم + عرض كلي عبر المطاعم. |
| تاريخ + مبلغ التجديد القادم | مفقود | محسوب من آخر دفعة + الفترة (إن وُجدت)، أو حقل صريح. |
| الحالة (مدفوع/قادم/متأخر) | مفقود | محسوبة: `paid` (دفعة سارية)، `upcoming` (تجديد خلال نافذة)، `overdue` (تجاوز التاريخ بلا دفعة). |

### ج) تحليلاتي (Owner business analytics)

| المؤشر | المصدر بعد بناء الفوترة |
|---|---|
| إجمالي المطاعم (نشط/معلّق/محذوف) | `restaurants` بعد `deleted_at` |
| مطاعم جديدة شهرياً (نمو + أعلى أشهر) | `group by date_trunc('month', created_at)` |
| الإيراد الإجمالي + بالشهر (تأسيس مقابل تجديدات) | `payments` group by شهر + نوع |
| أعلى الأشهر جلباً (مطاعم + إيراد) | ترتيب التجميعات |
| التجديدات القادمة (30/60/90 يوم) + المبلغ المتوقع | `payments`/`subscriptions` حسب `next_renewal_at` |
| المتأخرات | تجديدات تجاوزت التاريخ بلا دفعة |
| تفصيل حسب الخطة/الفروع | `group by plan, branch_count` |
| متوسط الإيراد لكل مطعم | إجمالي الإيراد ÷ عدد المطاعم |

ملاحظة عملة: الإيراد **متعدّد العملات** (18). يجب حسم: نجمّع لكل عملة على حدة (آمن، بلا أسعار صرف)، أم نوحّد على عملة عرض واحدة (يتطلّب أسعار صرف — تعقيد). **أرجّح: تجميع لكل عملة + إبراز عملة السوق الأساسية IQD.**

### د) سجل تدقيق

مفقود كلياً. المطلوب جدول `audit_log` يسجّل كل فعل أونر حسّاس (إنشاء/تعديل/تعليق/حذف مطعم، تسجيل دفعة، تغيير كلمة سر) مع: الفاعل (owner user id)، الفعل، الهدف (restaurant_id)، لقطة قبل/بعد (JSON)، الوقت.

---

## 5) نموذج بيانات الفوترة المقترح — «مقترح، لا يُطبّق الآن»

> هذه **مقترحات مخطط للنقاش فقط**. لن تُكتب أي migration قبل أن تحسم القسم 3.
> مبنية على **المسار C (هجين)** لأنه يغطّي A وB معاً؛ يُبسّط فوراً لو اخترت A.

### 5.1 حقول جديدة على `restaurants` (للتصنيف/التسعير)

```
plan           TEXT      -- اسم الخطة (مثلاً 'basic'/'pro') — أو enum لاحقاً
branch_count   INTEGER   -- عدّاد فروع للتسعير فقط (المسار i)، افتراضي 1
deleted_at     TIMESTAMPTZ  -- soft-delete؛ NULL = حيّ
```

### 5.2 جدول `payments` (دفتر الحسابات — جوهر تحليلات الإيراد)

```
id              UUID PK
restaurant_id   UUID FK → restaurants(id) ON DELETE CASCADE
kind            TEXT CHECK (kind IN ('initial','renewal','adjustment'))
amount          NUMERIC(12,2) NOT NULL
currency        TEXT NOT NULL            -- من lib/currencies.ts (18)
paid_at         TIMESTAMPTZ NOT NULL     -- تاريخ استلام الدفعة فعلياً
period_start    TIMESTAMPTZ              -- اختياري (للمسار B/C)
period_end      TIMESTAMPTZ              -- اختياري → يغذّي «التجديد القادم»
status          TEXT CHECK (status IN ('paid','upcoming','overdue')) DEFAULT 'paid'
note            TEXT                     -- مرجع يدوي (تحويل/إيصال)
recorded_by     UUID                     -- owner user id
created_at      TIMESTAMPTZ DEFAULT now()
INDEX (restaurant_id, paid_at)
INDEX (period_end) WHERE period_end IS NOT NULL   -- لاستعلامات التجديد
```

**اشتقاق الحالة/التجديد بلا cron** (تماشياً مع نمط lazy-revert في المشروع): «التجديد القادم» = أحدث `period_end` للمطعم؛ «متأخر» = `period_end < now()` بلا دفعة لاحقة. يُحسب وقت القراءة في صفحة الأونر، لا بمهمّة مجدولة.

### 5.3 (لو اخترت المسار B فقط) جدول `subscriptions` منفصل

```
id, restaurant_id, plan, amount, currency,
period TEXT CHECK (period IN ('monthly','yearly')),
started_at, next_renewal_at, status TEXT CHECK (status IN ('active','past_due','canceled'))
```
**توصيتي: لا تبنِه.** المسار C يجعل `payments` وحده كافياً عبر `period_end`؛ جدول اشتراكات منفصل يضاعف مصدر الحقيقة.

### 5.4 جدول `audit_log`

```
id            UUID PK
actor_id      UUID NOT NULL           -- owner user id
action        TEXT NOT NULL           -- 'restaurant.create' / 'payment.record' / ...
target_id     UUID                    -- restaurant_id غالباً
before        JSONB
after         JSONB
created_at    TIMESTAMPTZ DEFAULT now()
INDEX (created_at), INDEX (target_id)
```

كل الجداول الجديدة: **RLS «Owner full access» فقط** (نفس نمط `events`)، بلا أي سياسة قراءة عامة أو مستأجر.

---

## 6) الأمان (لا تُضعف العزل)

- **كل فعل/محمّل جديد يبدأ بـ`await requireOwner()`** — بلا استثناء (نمط C-1 قائم). الجداول الجديدة لا تُقرأ أبداً من سياق مستأجر أو عام.
- جداول الفوترة/التدقيق **خلف service-role + RLS owner-only** فقط؛ **لا** سياسة تكشفها لـ`anon`/`authenticated`/المستأجر. الفوترة بيانات مالية حسّاسة — لا تتسرّب لقائمة الزبون ولا للوحة المستأجر.
- التحقّق من المدخلات عند الحدود (نمط `accounts/actions.ts`: `UUID_RE`, `SLUG_RE`, تحقّق المبلغ/العملة/التاريخ). المبلغ يجب أن يكون موجباً وضمن `NUMERIC(12,2)`؛ العملة من `isSupportedCurrency`.
- soft-delete يجب أن يُخفي المطعم من القراءات العامة أيضاً: ربط `is_active`/`deleted_at` بسياسات RLS العامة (مطعم محذوف ناعماً = غير مقروء للزبون).
- سجل التدقيق نفسه للقراءة فقط من الأونر؛ لا فعل أونر يعدّله/يحذفه.
- لا تغيير على أنظمة المصادقة الثلاثة المنفصلة (CLAUDE.md: «do not unify them»).

---

## 7) خطة التنفيذ المرحلية المقترحة (للمراحل اللاحقة — ليست لهذه المرحلة)

1. **حسم القسم 3** (نموذج تجاري + معنى الفروع) — بوّابة بشرية، لا يبدأ شيء قبلها.
2. تحديث `prd.md`/`RULES.md` إن لزم (لو B/C أو فروع).
3. Migration الفوترة + الحقول الجديدة + soft-delete + audit_log (بعد موافقتك على المخطط).
4. توسيع إدارة المطاعم: قائمة موسّعة + فرز + تفاصيل مطعم + تعديل + soft-delete.
5. الفوترة: تسجيل دفعة + سجل + حالة/تجديد مشتق.
6. تحليلاتي: تبويب جديد بالمؤشرات (تجميع لكل عملة).
7. سجل التدقيق: تغليف الأفعال الحسّاسة + عارض.
8. توسيع التنقّل: من تبويبين إلى (نظرة عامة، المطاعم، الفوترة، التحليلات، السجل).

---

## 8) الفجوات الموجزة + القرارات التي أحتاجها منك

### الفجوات (تذكير)
- لا فوترة/إيراد/تجديدات/خطط/فروع إطلاقاً — جداول جديدة كاملة مطلوبة.
- إدارة المطاعم ناقصة: لا تفاصيل، لا تعديل، حذف صلب، لا فرز، تزويد ضيّق.
- لا سجل تدقيق.

### القرارات المطلوبة منك (قبل أي بناء)
1. **النموذج التجاري** (الأهم): المسار **A** (شراء لمرة واحدة، يلتزم الـPRD) أم **B** (اشتراكات، يغادر الـPRD ويتطلّب تحديثه) أم **C** (هجين، توصيتي)؟
2. **الفروع**: عدّاد تسعير فقط (i، مقبول) أم multi-branch فعلي (ii، يخالف §8)؟
3. **عملة التحليلات**: تجميع لكل عملة (توصيتي، بلا أسعار صرف) أم توحيد على عملة عرض (يتطلّب أسعار صرف)؟
4. **الحذف**: أحوّله إلى soft-delete افتراضياً مع إبقاء «حذف نهائي» كإجراء منفصل؟ (توصيتي: نعم.)
5. **«آخر نشاط»** في القائمة: نشاط الزبون (`events`) أم دخول الأدمن (`last_login_at`) أم كلاهما؟ (توصيتي: كلاهما.)
6. هل أوثّق التغيير في `prd.md`/`RULES.md` قبل الكود لو خرجنا عن النموذج الأصلي؟ (إجباري للمسار B/الفرع ii.)
