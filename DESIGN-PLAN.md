# خطة إعادة تصميم MenuPro — تدقيق شامل + خطة تنفيذ

> **النطاق:** تقرير وخطة فقط. **لم يُعدَّل أي كود.** الهدف: رفع الواجهة إلى مستوى عالمي مع احترام تعدد اللغات (عربي/كردي RTL + إنجليزي LTR)، mobile-first لجهة الزبون، Tailwind v4 + shadcn، وعدم كسر الوظائف القائمة.
>
> **الأساس الحالي:** Next.js 16 (App Router) · React 19 · Tailwind v4 · shadcn/ui (base-ui) · خطوط Tajawal/Inter/IBM Plex Mono · لوحة "editorial دافئة" (نبيذي `#8b1a1a` + ذهبي `#c9a961` + كريمي `#faf7f2`). يوجد `Design-System.html` مرجعي.
>
> **منهجية التدقيق:** قراءة كاملة لكل شاشات الزبون/الأدمن/المالك + مكوّنات shadcn، عبر أربعة مسارات متوازية (الزبون، الأدمن، المالك+النظام، الوصولية WCAG 2.2)، مع حساب نسب تباين فعلية.

---

## ملخّص تنفيذي — المشكلة الجذرية

ثلاث مشاكل بنيوية تُفسّر معظم النتائج، وإصلاحها في طبقة الـ tokens/primitives يرفع كل الشاشات دفعة واحدة:

1. **تصادم لوحتين (Two-palette collision) — High.** جهة الزبون *تُعيد التلوين فعلاً* لكل مطعم: `loadMenu` يحلّ `background_color / card_color / text_color / header_color / primary_color` وتستهلكها المكوّنات عبر `style={{...}}` inline. لكن كل ما يمرّ عبر رموز Tailwind (`bg-amber` لشارة الخصم، `text-gold` لإجمالي السلة، `bg-cream-deep` للـ placeholder، `text-muted-foreground`، والظلال `rgba(60,40,20,…)`) **مثبّت على لوحة Mesa النبيذية ولا يتبع براند المطعم**. النتيجة: مطعم بلون أزرق يحصل على أزرار زرقاء + شارات خصم نبيذية + إجمالي ذهبي + ظلال بنية. وأسوأ: اللون الافتراضي للـ theme-color أخضر `#16a34a` (`app/r/[slug]/layout.tsx:14`) بينما النظام كله نبيذي.

2. **لا يوجد مقياس طباعة ولا مقياس مسافات كـ tokens — High.** `globals.css` يعرّف مقياس `radius` ومقياس `shadow` كاملين، لكن **صفر رموز للحجم/ارتفاع السطر/الوزن وصفر رموز مسافات**. كل حجم خط اختيار يدوي (`text-[10px]`, `text-sm`, `text-3xl`) وكل فراغ قيمة لمرّة واحدة. لذلك «اسم المطعم» = حجم «عنوان بطاقة»، والهرمية محشورة في نطاق 14–18px.

3. **أهداف اللمس تحت الحد الأدنى في كل مكان — High.** `Button` الافتراضي `h-8` (32px)، و`sm` = `h-7` (28px)، و`lg` = `h-9` (36px). **أكبر زر غير أيقوني = 36px** — أقل من الحد 44px. زر «أضف للسلة» (أهم فعل في المنيو) = 32px، خطّافات السحب = 16px، وحدات +/− في السلة = 28px.

بالإضافة لها: shadcn مُثبّت لكن **جهة الزبون كلها مبنية يدوياً** (ثلاثة حوارات مختلفة، ثلاثة أشكال للسلة، إيموجي بدل أيقونات)، و`<img>` خام في كل مكان (لا `next/image`)، وحلقة focus بشفافية 50% (تباين ~2:1 = غير مرئية)، و`<html lang="ar">` لا يتغيّر عند تبديل اللغة (كل الأسماء الإنجليزية/الكردية تُنطق بقواعد عربية في قارئ الشاشة).

---

# الجزء أ — التدقيق (Audit)

النتائج مرتّبة حسب الأهمية ومجمّعة حسب المجال. الشدّة: **High** = يبدو مكسوراً/رخيصاً أو يفشل في الوصولية · **Medium** = قابل للتحسين بوضوح · **Low** = صقل.

## أ-1 · التلوين والتباين (Color & Contrast — WCAG AA)

| # | الشدّة | المشكلة | لماذا غير جميل/فاشل | الموضع |
|---|---|---|---|---|
| C1 | **High** | تصادم اللوحتين (انظر الملخّص). شارة الخصم `bg-amber`، إجمالي السلة `text-gold`، الـ placeholder `bg-cream-deep`، الظلال بنية — كلها لا تتبع براند المطعم | مطعم غير نبيذي يحصل على واجهة هجينة متناقضة هوياتياً | `menu-view.tsx:582,640,567`، `globals.css:53-57` |
| C2 | **High** | `muted-foreground #6b5e52` على الكريمي = **4.19:1** (يفشل 4.5)، وعلى الأبيض = **4.49:1** (حدّي/يفشل). يُستخدم للسعر الأصلي المشطوب، نص «لا أصناف»، تواريخ، أزمنة التحضير | نص ثانوي حقيقي (سعر، تاريخ) غير مقروء لضعاف البصر | كل الشاشات |
| C3 | **High** | الذهبي `#c9a961` على الكريمي = **2.36:1**، على الأبيض = **2.23:1** | تحية الترحيب الذهبية غير مقروءة | `welcome-screen.tsx:99`, `menu-view.tsx` |
| C4 | **High** | شارة الخصم: أبيض على `amber #d97706` = **2.85:1** عند `text-[10px]` bold | أهم معلومة تجارية (نسبة الخصم) غير مقروءة | `menu-view.tsx:582`, `product-view.tsx:88` |
| C5 | **High** | لا يوجد ضمان تباين للألوان التي يختارها المطعم: الأدمن يضبط 5 ألوان مستقلة دون أي تحقق أن النص مقروء على الخلفية | يستطيع صاحب المطعم بسهولة إنتاج منيو غير مقروء حرفياً | `design-view.tsx:154-160` |
| C6 | **Medium** | `--destructive #991b1b` و`--primary #8b1a1a` نبيذيان شبه متطابقين؛ وزر destructive = `bg-destructive/10 text-destructive` (تعبئة باهتة) فيبدو **أقل** إلحاحاً من الأساسي | فعل الحذف لا يحمل إشارة دلالية مميّزة على شاشة كثيرة الحذف | `button.tsx:18-19`, `confirm-dialog.tsx:51` |
| C7 | **Medium** | حدّ الإدخال `#e8dfd1` على الكريمي = **~1.2:1**، وحدّ الشِب الفرعي `border-border-lite #f0e8da` على الأبيض = **~1.15:1** (يفشل 1.4.11) | حدود الحقول/الشِبات غير مرئية حتى التركيز | `input.tsx:12`, `menu-view.tsx:527` |
| C8 | **Medium** | شارة الحالة `bg-olive/15 text-olive` — olive `#6b7f3f` على أبيض ≈ **3.9:1** عند `text-xs` (يفشل) | شارة «نشط» غير متوافقة AA | `accounts-table.tsx:88`, `dashboard/page.tsx:87` |
| C9 | **Medium** | لا رموز دلالية success/warning/error/info؛ النجاح مُرتجل بـ`text-olive` و`--chart-*` مكرّرة حرفياً من البراند بدل `var(--primary)` | غياب نظام حالات؛ انجراف صامت بين الرموز | `globals.css:98-106` |

## أ-2 · الطباعة (Typography)

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| T1 | **High** | لا مقياس طباعة كـ tokens؛ ثلاثة أحجام مختلفة لعنوان الصفحة عبر اللوحات (`text-base/lg/xl`)، واسم المطعم = حجم عنوان بطاقة | الهرمية مضغوطة في 14–18px، لا تباين حجم | `globals.css`، `layout.tsx:16`, `dashboard/page.tsx:60` |
| T2 | **محسوم ✅** | **القرار:** اعتماد **Vazirmatn** خطاً أساسياً (يدعم العربي والكردي السوراني بكامل الحروف `ێ ۆ ڕ ڵ گ چ پ ژ ڤ`، متوفّر على Google Fonts) مع **Noto Sans Arabic** كـfallback للكردي. يستبدل Tajawal الذي تغطيته للكردية غير مؤكّدة. (كان: High) | `layout.tsx:6-11` |
| T3 | **Medium** | أرقام الأسعار `font-bold tabular-nums` (خط sans) وليست `font-mono` رغم تجهيز `--font-mono`؛ نسيج «الفاتورة» غائب | السعر يفقد طابعه المقصود | `menu-view.tsx:593,640`, `cart-view.tsx:321` |
| T4 | **Medium** | أحجام دقيقة عشوائية `text-[10px]` (eyebrow، شارة الخصم، تسميات التحليلات) — تكسر المقياس وتحت حد قراءة العربية المريح | Tajawal عند 10px يتدهور (تفاصيل داخلية أكثر من اللاتيني) | `menu-view.tsx:250,296,582`, `analytics/page.tsx:133` |
| T5 | **Medium** | اسم المنتج `text-sm truncate` (سطر واحد) — اسم عربي بكلمتين ومُعدِّل يُقصّ بـ«…» | فقدان معلومة في عنصر أساسي | `menu-view.tsx:588` |
| T6 | **Low** | `tracking-tight` على `<h1>` العربي و`--font-heading` = `--font-sans` (لا استراتيجية اقتران) | letter-spacing سالب على الخط العربي المتّصل خطأ طباعي | `menu-view.tsx:253`, `globals.css:13` |

## أ-3 · التسلسل البصري والتخطيط (Hierarchy & Layout)

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| H1 | **High** | المقدّمة التحريرية تدفع المنيو تحت الطيّة: eyebrow بصيغة `WEDNESDAY · 14:22` (لاتيني CAPS داخل RTL) + تحية عامة ~120px فوق رفّ الأقسام | الزبون الجائع يرى ساعة ونصاً يشبه debug قبل أي طبق | `menu-view.tsx:247-256` |
| H2 | **High** | صفوف منيو الأدمن «حساء أزرار»: 4 أزرار في رأس القسم + 3 لكل منتج، كلها `size="sm"` بنفس الوزن، والحذف بنفس بروز التعديل | لا فعل أساسي، العين بلا راحة، الصف يقفز عند الالتفاف | `admin/.../menu-view.tsx:288-347` |
| H3 | **High** | جدول الحسابات بلا رأس/شريط أدوات حقيقي؛ زر الإنشاء يتيم داخل الجدول؛ لا عدّاد/بحث/فلتر | يقرأ كهيكل CRUD غير منمّق | `accounts/page.tsx:40`, `accounts-table.tsx:49` |
| H4 | **Medium** | لا hero/صورة شهية أعلى المنيو؛ أول عنصر بصري نص ثم شِبات ثم شبكة بطاقات صغيرة — لا نقطة تركيز | العين لا تستقرّ على شيء | منيو الزبون |
| H5 | **Medium** | رفّ «اختيارات الشيف» والشبكة الرئيسية بنفس معالجة البطاقة (حدّ/ظل/radius)؛ القسم «الخاص» لا يبدو خاصاً | فقدان إشارة التميّز للقسم المنسَّق/المخفَّض | `menu-view.tsx:300-327` |
| H6 | **Medium** | بطاقات إحصاء المالك «أرقام في صناديق» متطابقة بلا اتجاه/أيقونة/اتّجاه ترند | النمط المحظور في design-quality | `dashboard/page.tsx:51` |

## أ-4 · المسافات والإيقاع (Spacing & Rhythm)

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| S1 | **Medium** | padding أفقي غير متّسق `px-4` (الهيدر) مقابل `px-5` (المحتوى) على نفس الشاشة؛ المحتوى مزاح 4px داخل الهيدر | اختلال محاذاة الهامش بين الهيدر والجسم | `menu-view.tsx:214,249,321` |
| S2 | **Medium** | إيقاع عمودي ارتجالي (`pt-5 pb-2`, `pt-4 pb-3`, `pt-6`, `pb-32`) بلا مقياس؛ لا رموز مسافات | لا سُلّم؛ كل فجوة قيمة منفردة | `menu-view.tsx` عام |
| S3 | **Medium** | خلط radius رمزي/عشوائي: `rounded`، `rounded-lg`، `rounded-xl` داخل بطاقة واحدة؛ الزر `rounded-[min(var(--radius-md),10px)]` (سقف px على token) | لا لغة زوايا مشتركة بين الأسطح المتداخلة | `menu-view.tsx:110,147,319`, `button.tsx:25` |
| S4 | **Low** | الـ shadow scale جيد لكن `--shadow-cta` نبيذي يُطبَّق على *كل* زر أساسي حتى `sm` | توهّج 0 4px 14px على زر 28px غير متناسب؛ الجداول تبدو ثقيلة | `button.tsx:11`, `globals.css:57` |

## أ-5 · اتساق المكوّنات وshadcn

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| K1 | **High** | shadcn مُثبّت لكن جهة الزبون يدوية بالكامل: ثلاثة حوارات مختلفة (لغة الترحيب، اختيار اللغة، «اقرأ للنادل») بثلاث معالجات dismiss/ظل/حدّ | جذر التشتّت البصري بين الشاشات | `welcome-screen.tsx:119`, `menu-view.tsx:425`, `cart-view.tsx:411` |
| K2 | **High** | `<select>` و`<input type=checkbox>` خام: ثلاث معالجات select مختلفة، ومربّعات اختيار بلون نظام التشغيل الأزرق (تصطدم باللوحة الدافئة) | لا primitives `Select/Checkbox/Switch`؛ أكبر فجوة اتساق | `product-dialog.tsx:157`, `design-view.tsx:138`, `complementary-section.tsx:80` |
| K3 | **High** | تأكيدان مختلفان لنفس المهمّة: حذف المنيو عبر `ConfirmDialog` (على `Dialog` مع X زائد) وتبديل الوضع عبر `AlertDialog` الحقيقي؛ والحذف المدمِّر يستخدم `Dialog` لا `alertdialog` | منطق تأكيد مزدوج + dismiss مكرّر + دلالات SR ناقصة | `confirm-dialog.tsx` مقابل `modes-view.tsx:262` |
| K4 | **Medium** | ثلاثة أشكال للسلة (`CartBar` شريط داكن، `FloatingCart` حبّة 🛒، CTA السلة حبّة 📣) تتغيّر شكلاً ولوناً وموضعاً بين المنيو/المنتج | الزبون يفقد ثبات affordance السلة | `menu-view.tsx:635,661`, `cart-view.tsx:230` |
| K5 | **Medium** | إيموجي كأيقونات (🛒 📣 🗑 🚫 ⏱ ⭐ ✕) بجوار lucide نظيف؛ تختلف عبر Android/iOS | يبدو غير منتهٍ؛ لا نظام أيقونات | عام |
| K6 | **Medium** | مكوّن `Field` مكرّر 4–5 مرّات؛ شارة الحالة hand-rolled مرّتين؛ زر «خروج» raw `<button>` نصّي | تكرار + انجراف؛ لا `Badge` primitive | `category-dialog.tsx:81`+، `accounts-table.tsx:86`, `layout.tsx:11` |
| K7 | **Low** | أحجام Button (`xs/lg/icon`) معرّفة لكن غير مستخدمة؛ يُستعمل فقط `default`/`sm` | المقاس الوحيد القريب من المريح (lg) مهمَل | `button.tsx` |

## أ-6 · Mobile-first وأهداف اللمس

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| M1 | **High** | أهداف لمس تحت 44px في كل مكان: زر +للسلة 32px، +/− السلة 28px، خطّاف السحب 16px (يفشل حتى حد 24px في 2.5.8)، Button الافتراضي 32px | أكثر الأفعال استخداماً صغيرة وعرضة لخطأ اللمس | `menu-view.tsx:606`, `cart-view.tsx:305`, `sortable-list.tsx:105`, `button.tsx:23` |
| M2 | **Medium** | الـ bottom-nav بلا `env(safe-area-inset-bottom)` | يصطدم بمؤشّر الـ home في PWA على iOS/Android | `bottom-nav.tsx:16` |
| M3 | **Medium** | جدول 8 أعمدة بلا استراتيجية موبايل (overflow-x فقط) | لوح أفقي على الهاتف؛ لا تخطيط بطاقات | `accounts-table.tsx:62`, `table.tsx:7` |
| M4 | **Medium** | الفعل الأساسي «+ سكشن رئيسي» أعلى الشاشة (منطقة الإبهام الميتة) بينما الـ nav السهل للتنقّل فقط | بيئة الهاتف لا تُحترم | `admin/.../menu-view.tsx:89` |
| M5 | **Low** | `active:scale-95` على زر واحد فقط؛ بقية اللمسات بلا feedback | اللمس يبدو ميتاً | `menu-view.tsx:606` |

## أ-7 · صحّة وجمال RTL/LTR (حرج)

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| R1 | **High** | الهيدر `dir="ltr"` داخل صفحة RTL: الشعار يُثبَّت يساراً والأكشن يميناً لكل اللغات — معكوس للعربية/الكردية (السوق الأساسي) | أبرز نقطة في الشاشة تعرض اتجاهاً خاطئاً + يكسر focus order | `menu-view.tsx:213` |
| R2 | **High** | أسهم اتجاهية حرفية ثابتة: CTA الترحيب `... ←` وأزرار الرجوع `← ...` — لا تنعكس؛ لغة واحدة دائماً سهمها خاطئ | يجب استخدام أيقونة lucide تنعكس مع `dir` | `welcome-screen.tsx:113`, `product-view.tsx:71`, `cart-view.tsx:169` |
| R3 | **High** | زر إغلاق الحوار `absolute top-2 right-2` (فيزيائي) في كل حوارات shadcn | في RTL الإغلاق يُتوقّع يساراً؛ يجب `end-2` منطقي | `dialog.tsx:68` |
| R4 | **Medium** | السعر المخفَّض/المشطوب بثلاث معالجات مختلفة (عمودي بالبطاقة، inline بالمنتج، inline بالسلة) دون تدقيق RTL لتجاور `line-through` + لاحقة العملة | ثلاثة أشكال لمفهوم واحد؛ خطر تبادل بصري | `menu-view.tsx:592`, `product-view.tsx:97`, `cart-view.tsx:291` |
| R5 | **Medium** | العدّاد `dir="ltr" text-left` مثبّت يسار داخل بطاقة RTL بينما تسميته العربية يمين = زجزجة؛ وأرقام لاتينية بينما المشروع يستخدم هندية في مكان آخر | اتجاه + نظام أرقام غير متّسق داخل نفس الميزة | `countdown.tsx:45`, `modes-view.tsx:358` |

## أ-8 · الحركة والتفاعلات الدقيقة

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| A1 | **Medium** | شبه انعدام للحركة: `tw-animate-css` مستورد لكن الحيّ فيه فقط `active:scale-95` و`transition-colors`؛ شريط السلة يظهر/يختفي فجأة (يُرجِع `null`) | براند «editorial دافئ» يبدو جامداً ورخيصاً | `menu-view.tsx:631` عام |
| A2 | **Medium** | السحب-والإفلات: تغيير opacity إلى 0.6 فقط؛ لا ظل رفع، لا خط drop-indicator، رغم وجود `--shadow-lifted` | أهم تفاعل في محرّر المنيو يبدو مسطّحاً | `sortable-list.tsx:89` |
| A3 | **Medium** | تبديل الوضع (الميزة المركزية) صامت: الحلقة تقفز، لا toast، لا «يُطبَّق خلال 30 ثانية» | الفعل الأهم بلا تأكيد | `modes-view.tsx:124` |
| A4 | **Low** | لا نظام toast عام؛ كل نجاح/خطأ `<p>` inline يدفع التخطيط | رسائل تزيح التخطيط = الافتراضي غير المصقول | عام |
| A5 | **Low** | حركات الحوار غير مقيّدة بـ`prefers-reduced-motion` (2.3.3) | إزعاج لمستخدمي اضطراب الدهليز | `dialog.tsx:34`, `alert-dialog.tsx:31` |

## أ-9 · الوصولية WCAG 2.2 (تفصيل المعايير)

> جدول التباين الكامل والنتائج موجود ضمناً في أ-1. هنا المعايير غير اللونية.

| # | الشدّة | المعيار | المشكلة | الموضع |
|---|---|---|---|---|
| W1 | **High** | 3.1.2 Language of Parts | `<html lang="ar">` لا يتغيّر عند تبديل اللغة (يتحدّث state و`dir` على `<main>` فقط)؛ كل اسم إنجليزي/كردي يُنطق بقواعد عربية | `layout.tsx:38`, `menu-view.tsx:589` |
| W2 | **High** | 3.1.2 | spans أسماء `name_en/name_ku` لها `dir="ltr"` بلا `lang`؛ كذلك حقول الإدخال الإنجليزية | `admin/.../menu-view.tsx:281`, `product-dialog.tsx:70` |
| W3 | **High** | 2.4.7 / 2.4.11 / 1.4.11 | حلقة focus `ring-ring/50` (نبيذي 50%) ≈ **2:1** على الأبيض = غير مرئية في كل التطبيق؛ والشِبات بلا `focus-visible` صريح | `button.tsx:7`, `globals.css:155`, `menu-view.tsx:511` |
| W4 | **High** | 2.1.1 / 4.1.2 | الحوارات اليدوية (popup الترحيب، «اقرأ للنادل») بلا focus trap، لا نقل تركيز عند الفتح، بلا `aria-labelledby` | `welcome-screen.tsx:119`, `cart-view.tsx:411` |
| W5 | **High** | 2.5.8 / 2.1.1 | خطّاف السحب 16px (تحت 24px)؛ والسحب بالكيبورد موجود (dnd-kit) لكن بلا live region يعلن الموضع | `sortable-list.tsx:97` |
| W6 | **High** | 1.4.1 Use of Color | حالة الشِب النشط بلون فقط — لا حدّ/أيقونة/`aria-pressed`/`aria-selected` | `menu-view.tsx:513` |
| W7 | **Medium** | 4.1.2 | `LanguageDropdown` يكسر عقد listbox: كل `option` له `tabIndex={0}` بدل `aria-activedescendant` | `menu-view.tsx:432` |
| W8 | **Medium** | 1.3.1 / 2.4.6 | أقسام/شبكة المنتجات بلا عنوان؛ الأقسام والفرعية كلها `<h3>` (لا تمييز هرمي)؛ `CardTitle` = `<div>` لا heading | `menu-view.tsx:321`, `admin/.../menu-view.tsx:278`, `card.tsx:36` |
| W9 | **Medium** | 4.1.2 | أزرار toggle الإتاحة تقول «إخفاء/إظهار» بلا اسم المنتج في `aria-label` | `admin/.../menu-view.tsx:333` |
| W10 | **Medium** | 3.3.2 | منزلق المدّة بلا `aria-valuetext`/`<output>`؛ والمطلوب `*` بصري فقط بلا `sr-only (مطلوب)` | `closing-dialog.tsx:114`, `product-dialog.tsx:65` |
| W11 | **Medium** | 1.4.3 | placeholder بـ`muted-foreground` يفشل (4.49:1) في كل الحقول | `input.tsx:12` |

## أ-10 · عرض الصور

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| I1 | **High** | `next/image` غير مستخدم إطلاقاً؛ كل صورة `<img>` خام مع eslint مُعطّل: مصدر R2 800×800 يُحمَّل في فتحة 56px (thumbnail السلة)؛ لا srcset، لا blur، لا width/height ⇒ CLS + هدر egress (خطر مُعلَن في CLAUDE §9) | كل صورة تقفز عند الظهور؛ تكلفة R2 مهدورة | `menu-view.tsx:570`, `product-view.tsx:85`, `cart-view.tsx:279` |
| I2 | **Medium** | لا صورة = حرف كبير على بيج (أو مربّع بيج فارغ بالمنتج) | يبدو خطأ لا placeholder مقصود؛ والصور المفقودة شائعة بالمنيو | `menu-view.tsx:577`, `product-view.tsx:82` |

## أ-11 · الإحساس والهوية (الانطباع الأول)

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| E1 | **Medium** | أول ما يراه الزبون: ساعة وتحية عامة لا طعام (انظر H1) — لا «لحظة شهية» | يضعف الانطباع العاطفي في سياق ضيافة | welcome → menu |
| E2 | **Medium** | theme-color الافتراضي أخضر `#16a34a` يلفّ واجهة نبيذية | الهوية الافتراضية متناقضة داخلياً | `layout.tsx:14` |
| E3 | **Low** | «POWERED BY MESA OS» على شاشة ترحيب الزبون بـ`text-[10px]` | ختم المنصّة يُضعِف هوية المطعم في منتج one-time | `welcome-screen.tsx:115` |

## أ-12 · حالات الفراغ/التحميل/الخطأ + الميزة المركزية + التحليلات

| # | الشدّة | المشكلة | لماذا | الموضع |
|---|---|---|---|---|
| F1 | **High** | toggle إتاحة المنتج يبتلع الأخطاء بصمت ولا feedback عند النجاح — وهو يخفي أصنافاً عن زبائن أحياء | فشل صامت في فعل حسّاس | `admin/.../menu-view.tsx:333` |
| F2 | **High** | شاشة modes (الميزة المركزية) بطاقات عادية + إيموجي؛ Normal و Off **بنفس اللون** (`bg-muted`)؛ زر Off جملة شارحة كاملة داخل button؛ لا preview لما يراه الزبون | يجب أن تكون أكثر الشاشات تصميماً فصارت أقلّها | `modes-view.tsx:43-253` |
| F3 | **High** | التحليلات «أرقام في صناديق»: 7 صناديق بأعداد خام بلا bar/sparkline؛ وعمود «صورة؟» علامة ✓/✗ صغيرة رغم أنها هدف الشاشة؛ و`--chart-*` غير مستخدمة | النمط المحظور صراحةً في design-quality | `analytics/page.tsx:124-192` |
| F4 | **Medium** | skeleton المنيو يحاكي شاشة الترحيب لا الشبكة ⇒ قفزة تخطيط؛ وكل الـ skeletons بلوحة Mesa فتومض ثم تُعاد بلون المطعم | وميض لون عند كل تنقّل + قفزة | `r/[slug]/loading.tsx`، `admin/.../loading.tsx` |
| F5 | **Medium** | لا UI لخطأ الشبكة لدى الزبون: فشل قراءة عابر = نفس شاشة «غير متوفر» 🚫؛ فشل الـ polling يُبتلع بصمت بلا مؤشّر بيانات قديمة | الزبون يرى «المنيو غير متاح» عند أي تعثّر لحظي | `lib/menu.ts:171`, `menu-view.tsx:80` |
| F6 | **Medium** | منزلق المدّة في closing = range خام (إبهام نظام أزرق) بقراءة `{duration} h` بـ`text-xs` لاتيني؛ ولا بحث/عدّ في قوائم الاختيار المتعدّد | جودة أقلّ من شِبات الخصم المجاورة لها | `closing-dialog.tsx:114,152` |
| F7 | **Medium** | منتقيات اللون 5 بلا حواجز تباين ولا presets؛ `<input type=color>` خام؛ القائمة الحيّة جيدة لكن ساكنة بصنف مثبّت | يمكن إنتاج منيو غير مقروء؛ المنتقي غريب عن اللوحة | `design-view.tsx:154,308,327` |
| F8 | **Low** | حالات الفراغ نص عارٍ في صندوق؛ والـ pending `'...'` بدل spinner/تسمية وصفية (متّسق أحياناً فقط) | لحظات دفء/صقل ضائعة | عام |

**نقاط قوّة تستحقّ الحفاظ:** المعاينة الحيّة في تبويب التصميم (`design-view.tsx:327`) والعدّاد المُصحَّح للانحراف الزمني (`countdown.tsx`) مبنيّان جيداً — هما السقف الذي يجب أن تُقاس به شاشتا modes والتحليلات.

---

# الجزء ب — خطة إعادة التصميم

ملموسة وقابلة للتنفيذ ضمن Tailwind v4 + shadcn، تدريجياً ودون كسر الوظائف.

## ب-1 · نظام التصميم المقترح (Design Tokens)

### الفلسفة
لوحة «editorial دافئة» سليمة كاتجاه — نُبقيها ونُحكِمها: نبيذي + ذهبي + كريمي + حبر دافئ، بطابع مطعم راقٍ. نضيف: (أ) رموزاً دلالية مستقلّة عن البراند، (ب) مقياس طباعة ومسافات، (ج) آلية حقن براند المطعم بأمان تباين، (د) رموز حركة. ننتقل إلى **OKLCH** لاتّساق إدراكي ولتوليد مشتقّات اللون (hover/فعّال) برمجياً.

### (أ) ألوان أساسية (Primitives) — OKLCH

```css
@theme {
  /* === Brand: Burgundy ramp === */
  --burgundy-50:  oklch(0.96 0.012 25);
  --burgundy-100: oklch(0.90 0.03  25);
  --burgundy-300: oklch(0.62 0.12  25);
  --burgundy-500: oklch(0.45 0.155 25);   /* ≈ #8b1a1a الأساسي */
  --burgundy-700: oklch(0.37 0.15  25);   /* ضغط/فعّال */
  --burgundy-900: oklch(0.28 0.10  25);

  /* === Gold ramp (مشتقّ، للحوافّ التزيينية لا للنص الصغير) === */
  --gold-300: oklch(0.85 0.07 85);
  --gold-500: oklch(0.74 0.10 85);        /* ≈ #c9a961 */
  --gold-700: oklch(0.58 0.10 85);        /* نسخة نصّية تعبر AA على فاتح */

  /* === Warm neutrals === */
  --sand-50:  oklch(0.98 0.008 75);       /* ≈ #faf7f2 خلفية */
  --sand-100: oklch(0.96 0.012 75);       /* ≈ #f3eee4 */
  --sand-200: oklch(0.92 0.015 75);       /* ≈ #e8dfd1 حدود */
  --sand-300: oklch(0.86 0.018 75);
  --ink-900:  oklch(0.22 0.01 60);        /* ≈ #1a1a1a نص */
  --ink-600:  oklch(0.50 0.02 60);        /* نص ثانوي يعبر AA (بديل muted-fg) */

  /* === Semantic state hues (مستقلّة عن البراند) === */
  --green-600:  oklch(0.55 0.13 150);     /* success */
  --amber-600:  oklch(0.62 0.15 70);      /* warning */
  --red-600:    oklch(0.52 0.20 27);      /* error/destructive — أحمر نقي مميَّز عن النبيذي */
  --blue-600:   oklch(0.50 0.13 245);     /* info */
}
```

### (ب) رموز دلالية (Semantic tokens) — ما تستهلكه المكوّنات

```css
@theme inline {
  --color-background:        var(--sand-50);
  --color-foreground:        var(--ink-900);
  --color-muted-foreground:  var(--ink-600);   /* يرفع التباين فوق 4.5:1 — يحلّ C2/W11 */
  --color-card:              #ffffff;
  --color-border:            var(--sand-200);
  --color-border-strong:     var(--sand-300);  /* حدود مرئية للحقول — يحلّ C7 */

  --color-primary:           var(--burgundy-500);
  --color-primary-hover:     var(--burgundy-700);
  --color-primary-foreground:#ffffff;
  --color-accent:            var(--gold-500);
  --color-accent-foreground: var(--ink-900);
  --color-accent-text:       var(--gold-700);  /* للنص الذهبي فقط (يعبر AA) — يحلّ C3 */

  /* حالات دلالية + foreground مضمون التباين */
  --color-success:    var(--green-600);  --color-success-foreground: #fff;
  --color-warning:    var(--amber-600);  --color-warning-foreground: var(--ink-900);
  --color-destructive:var(--red-600);    --color-destructive-foreground: #fff; /* يحلّ C6 */
  --color-info:       var(--blue-600);   --color-info-foreground: #fff;

  --color-ring: var(--burgundy-500);     /* تُستخدم بكثافة كاملة لا /50 — يحلّ W3 */
}
```

> **شارة الخصم (C4):** تُعاد إلى `--color-destructive`/`--color-primary` بتعبئة صلبة ونص أبيض (≥4.5:1)، لا `bg-amber` الباهت.

### (ج) حقن براند المطعم بأمان (يحلّ C1/C5/E2)

المبدأ: **لون واحد للبراند** (`primary_color`) يُحقن كمتغيّر CSS على جذر صفحة الزبون، وتُشتقّ منه المشتقّات، وتُربط به الرموز التي تتبع البراند (الأزرار، الحلقة، الـ accents) — بينما تبقى **الرموز الدلالية للحالة ثابتة** (الأخضر/الأحمر يجب ألا يتبعا البراند).

```tsx
// في app/r/[slug]/menu-view.tsx (مفهومياً) — حقن مرّة واحدة على الحاوية
<main style={{
  ['--color-primary']: brand.primary,          // من primary_color
  ['--color-primary-foreground']: pickReadable(brand.primary), // أبيض/حبر حسب التباين
  ['--color-background']: brand.background,
  ['--color-card']: brand.card,
  ['--color-foreground']: brand.text,
} as CSSProperties}>
```

ثم يُعاد توجيه `bg-amber`/`text-gold`/`bg-cream-deep`/الظلال إلى رموز تتبع البراند أو إلى رموز محايدة دافئة لا تتعارض. **حواجز التباين:** في تبويب التصميم يُحسب تباين `text` على `background`/`card` و`primary` على `card` لحظياً (نفس صيغة WCAG)، ويُعرض تحذير + اقتراح presets جاهزة (نزرعها من اللوحة الحالية) — يمنع المطعم من إنتاج منيو غير مقروء.

### (د) مقياس الطباعة (يحلّ T1/T3/T4/T5)

```css
@theme {
  /* أحجام سائلة: تكبر على الأكبر، تبقى مقروءة على الهاتف. لا قيم تحت 12px للنصوص الحقيقية */
  --text-caption: clamp(0.75rem, 0.72rem + 0.15vw, 0.8125rem);  /* 12–13 */
  --text-body:    clamp(0.9375rem, 0.9rem + 0.2vw, 1rem);       /* 15–16 */
  --text-lead:    clamp(1.0625rem, 1rem + 0.3vw, 1.1875rem);
  --text-h3:      clamp(1.1875rem, 1.05rem + 0.5vw, 1.375rem);
  --text-h2:      clamp(1.375rem, 1.15rem + 0.9vw, 1.75rem);
  --text-h1:      clamp(1.75rem, 1.3rem + 1.8vw, 2.5rem);
  --text-display: clamp(2.25rem, 1.5rem + 3vw, 3.5rem);         /* hero الترحيب */

  --leading-tight: 1.2;   /* عناوين */
  --leading-snug:  1.4;
  --leading-relaxed: 1.7; /* نص عربي طويل — العربية تحتاج سطوراً أعلى */

  --font-numeric: var(--font-mono); /* الأسعار/العدّاد ⇒ IBM Plex Mono فعلاً */
}
```

**اقتران الخطوط (محسوم ✅):** الخط الأساسي للعناوين والجسم = **Vazirmatn** (يدعم العربي والكردي السوراني بالكامل، Google Fonts)، مع **Noto Sans Arabic** كـfallback للكردي، و**IBM Plex Mono** للأرقام/الأسعار/العدّاد. يستبدل Tajawal. قاعدة: عائلتان نصّيتان كحدّ أقصى (Vazirmatn + Mono)، `font-display: swap`، subset عربي/لاتيني، وعدم وضع `tracking` سالب على العربية (يحلّ T6).

```css
@theme {
  --font-sans: "Vazirmatn", "Noto Sans Arabic", system-ui, sans-serif;
  --font-mono: "IBM Plex Mono", ui-monospace, monospace;
  --font-heading: var(--font-sans); /* نفس Vazirmatn بأوزان أثقل للعناوين */
}
```
> في التنفيذ: استبدال `Tajawal` في `app/layout.tsx` بـ`Vazirmatn` عبر `next/font/google` (subsets: `['arabic','latin']`، الأوزان 400/500/700/800)، وإضافة `Noto_Sans_Arabic` كـfallback.

### (هـ) مقياس المسافات (يحلّ S1/S2)

```css
@theme {
  --space-gutter:  1rem;     /* هامش الصفحة الموحَّد — هيدر ومحتوى معاً (يحلّ S1) */
  --space-card:    0.875rem; /* padding داخلي للبطاقة */
  --space-stack:   0.75rem;  /* فجوة عناصر متجاورة */
  --space-section: clamp(1.5rem, 1rem + 2vw, 2.5rem); /* بين أقسام المنيو */
  --space-safe-b:  env(safe-area-inset-bottom); /* يحلّ M2 */
}
```
قاعدة تنفيذ: padding أفقي واحد (`px-[--space-gutter]`) للهيدر والجسم. سُلّم عمودي من خطوات المقياس فقط — لا قيم مفردة.

### (و) Radius / Shadow / Motion

- **Radius:** المقياس الحالي ممتاز — نُلزِم لغة موحّدة: بطاقات/حوارات `--radius-2xl`، حقول/أزرار `--radius-lg`، شِبات `--radius-full`. لا `rounded` العاري ولا سقوف px على tokens (يحلّ S3).
- **Shadow:** نُبقي المقياس. نقصر `--shadow-cta` على الأزرار الأساسية بحجم `lg` فقط (يحلّ S4)، ونستخدم `--shadow-lifted` لعنصر السحب المرفوع (يحلّ A2).
- **Motion (جديد):**
```css
@theme {
  --duration-fast: 150ms;  --duration-normal: 250ms;  --duration-slow: 400ms;
  --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-spring:   cubic-bezier(0.34, 1.56, 0.64, 1);
}
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation-duration: .01ms !important; transition-duration: .01ms !important; }
}  /* يحلّ A5 */
```

### (ز) حذف الكود الميّت
حذف/استبدال كتلة `.dark` المحايدة الرمادية (ليست لوحة Mesa) وكل `dark:` المعطّلة في `button.tsx`/`input.tsx`. إمّا حذفها أو بناء dark Mesa حقيقي لاحقاً.

---

## ب-2 · توجيه على مستوى كل شاشة

### شاشات الزبون (الأولوية القصوى — mobile-first)

**`/r/[slug]` المنيو:**
- استبدال المقدّمة `WEDNESDAY · 14:22` + التحية بـ **شريط براند/hero مقتضب**: شعار + اسم المطعم (heading حقيقي) على خلفية اللون أو صورة غلاف اختيارية؛ إزالة الساعة (يحلّ H1/E1).
- هيدر منطقي الاتجاه (إزالة `dir="ltr"`): الشعار في الـ start، الأكشن في الـ end — ينعكس تلقائياً (يحلّ R1).
- رفّ الأقسام: شِبات بحالة نشطة **مزدوجة الإشارة** (تعبئة + حدّ + `aria-selected`) وfocus مرئي (يحلّ W6/W3)، أهداف ≥44px.
- بطاقة المنتج: صورة `next/image` بنِسَب ثابتة وblur-up، اسم بسطرين (`line-clamp-2` لا truncate)، سعر `--font-numeric`، زر «+» ≥44px مع feedback (يحلّ I1/T5/M1/M5).
- **اختيارات الشيف / Closing:** معالجة أغنى من الشبكة العادية (صورة أكبر/سطح accent/شريط)، و**عدّاد تنازلي للزبون** من `closing_mode_ends_at` (البيانات موجودة وغير معروضة) + لافتة «ينتهي قريباً» عند الاقتراب (يحلّ H5/F2-diner).
- placeholder صورة مصمَّم (مونوغرام/أيقونة شوكة) بدل الحرف على بيج (يحلّ I2).

**welcome-screen:** hero بحجم `--text-display`، CTA بأيقونة lucide تنعكس مع الاتجاه (يحلّ R2)، تقليل ختم المنصّة (E3). حوار اللغة عبر `Dialog` shadcn (focus trap + `aria-labelledby`) بدل اليدوي (يحلّ W4).

**closed-screen:** تمييز «مغلق» عن «خطأ شبكة»: شاشة مطمئنة لخطأ عابر مع إعادة محاولة، منفصلة عن «المطعم غير نشط» (يحلّ F5). أيقونة lucide بدل 🚫.

**`/p/[productId]`:** صورة hero `next/image` priority، سعر/خصم بمعالجة RTL موحّدة (يحلّ R4)، زرّ رجوع منطقي، CTA سلة موحّد الشكل مع باقي الشاشات (يحلّ K4).

**`/cart` + «اقرأ للنادل» + الاقتراحات:** وحدات +/− ≥44px بـ`−/+` كرموز و`aria-label` منفصل (يحلّ M1)؛ thumbnails `next/image` 56px فعلية؛ حوار «اقرأ للنادل» عبر `Dialog` (focus trap, `aria-labelledby`, زر إغلاق مسمّى) (يحلّ W4)؛ حالة سلة فارغة مصمّمة مع CTA للعودة.

### لوحة الأدمن

**menu:** تحويل «حساء الأزرار» إلى فعل أساسي واحد ظاهر + قائمة `⋮` overflow للثانوي/المدمِّر (يحلّ H2)؛ نقل «+ سكشن» إلى FAB سفلي قابل للوصول بالإبهام (يحلّ M4)؛ خطّاف سحب ≥24px مع ظل رفع + live region للموضع (يحلّ M1/A2/W5)؛ `aria-label` يحوي اسم المنتج على toggle، وفقّاعات نجاح/خطأ بدل الفشل الصامت (يحلّ W9/F1).

**modes (يجب أن تكون بطلة التطبيق — F2):** بطاقات بألوان دلالية مميّزة (Normal = نجاح هادئ، Closing = primary، **Off = تحذير/محايد مميّز** لا نفس Normal)، نظام أيقونات lucide بدل الإيموجي، و**mini-preview** لما يراه الزبون لكل وضع، وtoast «طُبِّق — يظهر للزبون خلال ~30 ثانية» عند التبديل (يحلّ A3). closing-dialog: استبدال الـ range الخام بشِبات presets (2/4/حتى الإغلاق) متطابقة جودةً مع شِبات الخصم، وإبراز سطر تحوّل السعر، وبحث/عدّ في القائمة (يحلّ F6)، و`aria-valuetext`/`<output>` (يحلّ W10).

**design:** منتقيات لون مع **حواجز تباين حيّة + presets** (يحلّ C5/F7)، معاينة تعكس منتجاً حقيقياً.

**analytics (F3):** استبدال الصناديق السبعة بـ **bar/sparkline** عبر `--chart-*`، وجدول المنتجات بأشرطة مقدار + تجميع «بصورة/بلا صورة» يجيب مباشرةً عن سؤال الشاشة.

### لوحة المالك
- جدول حسابات حقيقي: شريط أدوات (عدّاد + بحث + فعل أساسي)، رأس لاصق، تخطيط **بطاقات على الموبايل** بدل overflow أفقي (يحلّ H3/M3/أ-9 جدول)، `Badge` للحالة (يحلّ C8/K6).
- شاشة دخول بهوية بصرية (شعار/اتجاه) بدل الصندوق العام.
- `loading.tsx`/Suspense skeletons (لا شاشة فارغة أثناء جلب RSC).

---

## ب-3 · توصيات على مستوى المكوّنات

| المكوّن | الإجراء |
|---|---|
| **Button** (`button.tsx`) | إعادة مقاسات لحدّ لمس: `default → h-11` (44px)، `sm → h-9`، `lg → h-12`، `icon → size-11`؛ حلقة focus صلبة عالية التباين (`ring-2 ring-offset-2`)؛ قصر `shadow-cta` على `lg`؛ `destructive` تعبئة صلبة حمراء مميَّزة عن النبيذي. **يحلّ M1/W3/S4/C6 في كل التطبيق.** |
| **Input** (`input.tsx`) | `h-11`، `border-border-strong` (تباين ≥3:1)، `placeholder` بـ`ink-600`. يحلّ M1/C7/W11. |
| **Dialog/AlertDialog** | زر الإغلاق `end-2` منطقي (يحلّ R3)؛ توحيد التأكيدات على `AlertDialog` للمدمِّر (يحلّ K3)؛ حركات مقيّدة بـreduced-motion. |
| **Select / Checkbox / Switch** (جديد) | primitives shadcn على البراند بدل الخام الأزرق؛ تستبدل المعالجات الثلاث (يحلّ K2). |
| **Badge** (جديد) | شارة حالة واحدة بألوان دلالية — تستبدل الـ pill المكرّر (يحلّ K6/C8). |
| **Chip** (جديد، مشترك) | شِب أقسام بحالة مزدوجة الإشارة + `aria-selected` + focus + ≥44px (يحلّ W6/M1) — يُوحّد رفوف الزبون. |
| **Toast** (جديد) | نظام إشعارات عام للنجاح/الخطأ يستبدل `<p>` inline (يحلّ A4/F1/A3). |
| **Field** (مشترك) | استخراج المكوّن المكرّر 5× مع `id/for` صريح + `*` ‏`sr-only` (يحلّ K6/W10). |
| **PriceTag** (جديد) | عرض سعر/خصم/مشطوب موحّد بـ`--font-numeric` ومعالجة RTL واحدة (يحلّ R4/T3). |
| **MenuImage** (جديد) | غلاف `next/image` بنِسَب/أحجام/placeholder موحّدة (يحلّ I1/I2). |
| **EmptyState** (جديد) | أيقونة + نص + CTA — لكل حالات الفراغ (يحلّ F8). |
| **CountdownBadge** | إعادة استخدامه لدى الزبون؛ أرقام هندية متّسقة؛ تصعيد اللون قرب الانتهاء فقط (يحلّ R5/A-countdown). |
| **بنية اللغة** | تحديث `document.documentElement.lang/dir` عند التبديل + `lang` على spans متعدّدة اللغة (يحلّ W1/W2/W7). |

---

## ب-4 · خارطة طريق تنفيذية على مراحل

مرتّبة: **أكبر أثر بصري بأقل خطر أولاً.** كل مرحلة تُسلَّم وتُختبر مستقلّةً، ولا تكسر الوظائف.

### المرحلة 0 — أساس الـ Tokens والـ Primitives (أعلى أثر/أقل خطر)
*لا تغييرات تخطيط — فقط رموز ومقاسات، فترتقي كل الشاشات فوراً.*
1. كتابة نظام الـ tokens الجديد في `globals.css` (ألوان OKLCH + دلالية + طباعة + مسافات + حركة)، مع إبقاء الأسماء القديمة كـaliases مؤقّتة لتفادي الكسر.
2. إصلاح `button.tsx` و`input.tsx`: المقاسات (44px)، حلقة focus، destructive، shadow. **يحلّ دفعةً: M1, W3, C6, S4, C7, W11.**
3. إصلاح `dialog.tsx`: `end-2` + reduced-motion. **يحلّ R3, A5.**
4. رفع `muted-foreground`/الذهبي النصّي/حدود الحقول لعبور AA. **يحلّ C2, C3, C7.**
5. رفع `<html lang/dir>` ديناميكياً + `lang` على الـ spans. **يحلّ W1, W2.**

### المرحلة 1 — جهة الزبون (أعلى قيمة بصرية ظاهرة)
6. حقن براند المطعم الموحّد + إعادة توجيه `amber/gold/cream/الظلال`. **يحلّ C1, C4, E2.**
7. هيدر منطقي الاتجاه + أيقونات أسهم تنعكس + إزالة الإيموجي لصالح lucide. **يحلّ R1, R2, K5.**
8. `MenuImage` (`next/image`) + placeholder مصمَّم + `PriceTag` + `Chip`. **يحلّ I1, I2, R4, W6, T3.**
9. استبدال المقدّمة بـhero/براند، بطاقة منتج بسطرين، أهداف لمس. **يحلّ H1, H4, T5, E1.**
10. توحيد الحوارات على `Dialog` (focus trap) + توحيد CTA السلة + وحدات +/− ≥44px. **يحلّ K1, K4, W4, M1.**
11. إثراء اختيارات الشيف/Closing + عدّاد الزبون + حالات فراغ/خطأ مصمّمة. **يحلّ H5, F5, F8.**

### المرحلة 2 — لوحة الأدمن (الميزة المركزية والتحليلات)
12. `Select/Checkbox/Switch/Badge/Toast` + استخراج `Field`. **يحلّ K2, K6, A4.**
13. إعادة تصميم menu: فعل أساسي + overflow + FAB + خطّاف سحب a11y + feedback. **يحلّ H2, M4, A2, W5, W9, F1.**
14. **modes كبطلة:** ألوان دلالية + أيقونات + mini-preview + toast التبديل + closing-dialog presets/بحث/output. **يحلّ F2, A3, F6, W10.**
15. design: حواجز تباين + presets. **يحلّ C5, F7.**
16. analytics: bar/sparkline + جدول مقدار/تجميع الصور. **يحلّ F3.**
17. safe-area للـ bottom-nav + توحيد skeletons. **يحلّ M2, F4.**

### المرحلة 3 — لوحة المالك
18. جدول حسابات (شريط أدوات + بطاقات موبايل + Badge + رأس لاصق) + دخول بهوية + skeletons. **يحلّ H3, M3, H6, C8.**

### المرحلة 4 — صقل الحركة والوصولية
19. حركات هادفة (ظهور شريط السلة، تبديل الأقسام، السحب) ضمن رموز المدّة/المنحنى. **يحلّ A1, A2.**
20. تدقيق هرمية العناوين/landmarks + `Chef's Selection` مترجَم + `aria` للحوارات/listbox. **يحلّ W7, W8, T4.**
21. تدقيق وصولية ختامي بأداة آلية + كيبورد + قارئ شاشة عربي/كردي/إنجليزي.

---

## مصفوفة الأثر مقابل الجهد (لتحديد البدء)

| العنصر | الأثر البصري | الجهد | الخطر |
|---|---|---|---|
| المرحلة 0 (tokens + primitives) | ⭐⭐⭐⭐⭐ | متوسط | منخفض |
| حقن البراند + إعادة توجيه الألوان (6) | ⭐⭐⭐⭐⭐ | متوسط | منخفض |
| `next/image` + بطاقة المنتج (8,9) | ⭐⭐⭐⭐ | متوسط | منخفض |
| هيدر/اتجاه RTL + أيقونات (7) | ⭐⭐⭐⭐ | منخفض | منخفض |
| modes كبطلة (14) | ⭐⭐⭐⭐ | عالٍ | متوسط |
| analytics مرئية (16) | ⭐⭐⭐ | متوسط | منخفض |
| جدول المالك (18) | ⭐⭐⭐ | متوسط | منخفض |

**التوصية:** ابدأ بالمرحلة 0 ثم البندين 6 و7 — ثلاث دفعات منخفضة الخطر ترفع *كل* شاشة قبل لمس أي تخطيط، وتُري Mustafa فرقاً جذرياً بأقل مخاطرة على الوظائف القائمة.
